# PIT 警車追逐遊戲 Implementation Plan

## 1. Goal Description

建立一款可在桌面瀏覽器遊玩的 3D 警車 PIT（Precision Immobilization Technique）追逐模擬遊戲。玩家使用鍵盤方向鍵控制警車，追逐由 AI 駕駛的嫌犯車輛，並可在駕駛第一人稱、自動跟車等視角之間切換。進入追逐前，玩家可以選擇場景、白天／黑夜、天氣與警車種類；不同環境及車輛會實際改變駕駛體驗。

第一版目標是完成可重複遊玩的核心循環：設定追逐條件、選擇警車、開始追逐、控制警車、切換鏡頭、追上嫌犯、執行 PIT、依結果判定成功或失敗，以及重新開始。第一版提供城市、鄉間與高速公路三種場景，晴朗、下雨與下雪三種天氣，以及白天／黑夜切換。

遊戲將以正式發布的網頁版為主要交付成果，玩家可從其他電腦透過公開網址直接遊玩，不需要使用 `localhost`。同時加入 PWA（Progressive Web App）支援，讓玩家可從瀏覽器將遊戲安裝至桌面／開始功能表並以獨立視窗啟動；第一版暫不另外建立 Windows `.exe`，但保留日後以 Tauri 或 Electron 包裝的相容性。

### 研究結論與影響範圍

- 工作區目前為空，沒有既有原始碼、套件、資料或 Git 工作樹需要相容。
- 未找到 `PROJECT_SOUL.md`，因此目前沒有已定義的作品定位衝突。
- 預計以 React、TypeScript、Three.js（透過 React Three Fiber）和瀏覽器端物理引擎製作單頁 3D 遊戲。
- 第一版不依賴後端；遊戲狀態與設定保留在瀏覽器端，避免引入不必要的資料模型與部署複雜度。

## 2. Open Questions / User Review Required

已確認的設計決策：

1. **PIT 操作方式**：不設獨立攻擊鍵，玩家需以真實駕駛方式碰撞嫌犯車輛後側四分之一處；物理結果與接觸角度共同決定是否成功。
2. **第一版遊戲範圍**：單一嫌犯車、每局 60 秒；暫不加入武器、多人連線、行人與永久進度，後續再擴充。
3. **場景選擇**：進入遊戲前可選城市、鄉間或高速公路。
4. **環境選擇**：進入遊戲前可選白天／黑夜，以及晴朗／下雨／下雪。
5. **天氣物理效果**：下雨降低抓地力，下雪進一步降低抓地力並增加煞車距離；效果同時套用至玩家與 AI 車輛，以維持公平性。
6. **警車選擇**：第一版提供三種具有不同駕駛特性的警車：巡邏轎車（均衡）、攔截跑車（高速、較敏感）、警用 SUV（穩定、較重但加速較慢）。先以原創通用車型呈現，不使用真實車廠商標。
7. **視覺方向**：採帶有街機速度感的寫實低多邊形 3D；各場景具有清楚不同的地標、道路寬度、彎道密度與環境配色。
8. **發布方式**：第一版正式部署為公開網頁，並提供可安裝 PWA；`localhost` 僅用於開發驗證，不是玩家入口。
9. **開發方式**：採分段實作，每一里程碑完成後先建置、測試與瀏覽器驗證，再進入下一段，以降低跨系統問題的除錯難度。

目前沒有尚待決定且會阻礙第一版實作的問題。若同意以上具體化設計，請明確回覆「同意」。

## 3. Proposed Changes

由於目前尚無專案檔案，以下是核准後預計建立的檔案與責任劃分。實際初始化產生的設定檔名稱可能依 Sites 樣板略有調整，但不會改變功能範圍。

### 專案與部署設定

- `package.json`
  - 建立開發、建置與檢查指令。
  - 加入 React、TypeScript、Three.js、React Three Fiber、輔助 3D 工具與物理引擎依賴。
- `.openai/hosting.json`
  - 建立 Sites 相容的靜態／前端部署設定，不加入資料庫或登入依賴。
- `public/manifest.webmanifest`
  - 定義遊戲名稱、圖示、主題色、橫向顯示及獨立視窗啟動方式，提供桌面安裝體驗。
- `public/sw.js`（或建置工具產生的 service worker）
  - 快取遊戲殼層與必要靜態資源，使已安裝版本在資源成功快取後可離線啟動。
  - 採版本化快取與安全更新策略，避免舊版資源和新版程式互相混用。
- TypeScript、Vite／Sites 相關設定檔
  - 啟用嚴格型別檢查與可部署的建置輸出。

### 頁面與整體介面

- `app/page.tsx`（或樣板對應的主要頁面檔）
  - 建立全螢幕遊戲入口、追逐設定畫面、遊戲 HUD、教學提示、暫停／結算與重新開始流程。
  - 設定畫面提供場景、日夜、天氣及警車卡片；開始前呈現各選項的特性與抓地力提示。
  - 顯示速度、與嫌犯距離、剩餘時間、PIT 接觸提示、目前視角及任務狀態。
- `app/layout.tsx`
  - 設定遊戲名稱、描述、語系與分享資訊。
- `app/globals.css`
  - 建立響應式 HUD、警燈色彩、可讀性層級、按鈕和狀態轉場。
  - 動畫以 class 與 CSS transition 為主，不使用不必要的 `!important` 或高頻 inline style。

### 3D 遊戲場景

- `components/game/PitGame.tsx`
  - 管理 Canvas、遊戲循環、場景生命週期與各系統整合。
- `components/game/World.tsx`
  - 根據選擇載入城市、鄉間或高速公路配置，共用物理道路與基礎材質，同時替換環境物件與道路特徵。
  - 城市：較密集建築、街燈、較窄道路與較多彎道；鄉間：田野、樹木、雙向道路與連續彎；高速公路：寬車道、護欄、交流道意象與較高巡航速度。
  - 使用可重用幾何與 instancing 控制 draw calls，避免大量獨立 DOM／3D 物件。
- `components/game/Environment.tsx`
  - 根據白天／黑夜調整天空、主光源、環境光、霧與車燈可見度。
  - 根據晴朗／下雨／下雪套用粒子、路面濕潤度、能見度與環境色調；粒子效果限制數量並重用緩衝區，避免逐幀建立物件。
- `components/game/VehicleModel.tsx`
  - 以共用低多邊形零件建立三種原創警車外形，保留一致的碰撞介面、輪胎與警燈掛點。
- `components/game/PoliceCar.tsx`
  - 建立玩家警車、加速／煞車／倒車／轉向、速度限制、抓地力與警燈效果。
  - 依巡邏轎車、攔截跑車或警用 SUV 套用不同質量、加速、極速、轉向與穩定性參數。
  - 將晴朗、雨天與雪天的路面係數套用至輪胎側向抓地力、加速與煞車表現。
  - 方向鍵：上加速、下煞車／倒車、左右轉向；避免頁面跟著方向鍵捲動。
- `components/game/SuspectCar.tsx`
  - 建立嫌犯車 AI：沿道路前進、保持速度、受阻迴避、依玩家距離調整策略，以及遭撞擊後的物理反應。
  - 使用有限狀態機區分巡航、逃逸、閃避、失控與被制伏狀態，確保行為可測試。
  - AI 依場景道路速度與天氣抓地力調整目標速度、煞車距離和轉向幅度，不以作弊方式忽略濕滑路面。
- `components/game/CameraRig.tsx`
  - 提供至少三種視角：駕駛第一人稱、自動跟車追蹤、後方第三人稱。
  - 使用 `C` 鍵循環切換；鏡頭位置與朝向以平滑阻尼更新，避免突跳。
- `components/game/PitSystem.tsx` 或對應邏輯模組
  - 根據接觸位置、相對速度、碰撞角度和嫌犯車旋轉量判定有效 PIT。
  - 加入短暫判定冷卻，避免單次碰撞被重複計分。

### 狀態、輸入與遊戲規則

- `hooks/useKeyboardControls.ts`
  - 集中註冊與清理鍵盤事件，處理方向鍵、視角切換、暫停與重新開始。
  - 將高頻輸入留在遊戲 render loop 中讀取，不以 React state 逐幀重繪介面。
- `store/gameStore.ts`
  - 管理場景、日夜、天氣、警車選擇，以及開始、進行中、暫停、成功、失敗、倒數、分數與鏡頭模式。
  - 將高頻物理狀態與低頻 UI 狀態分離，降低不必要的 React render。
- `lib/gameConfig.ts`
  - 集中場景設定、天氣抓地力係數、三種警車參數、時限、AI 難度、PIT 判定門檻與鏡頭設定，避免散落的 magic numbers。
- `lib/sceneConfig.ts`
  - 定義城市、鄉間與高速公路的道路寬度、彎道、建議速度、環境資產與生成規則，讓場景差異可擴充且不複製遊戲邏輯。
- `lib/gameRules.ts`
  - 實作可獨立測試的 PIT 成功條件、失敗條件與評分規則。

### 測試

- `lib/gameRules.test.ts`
  - 測試有效／無效撞擊角度、速度差、接觸區域、重複判定冷卻與勝負條件。
  - 測試雨雪抓地力係數、不同車重對 PIT 門檻的影響，以及設定重啟後是否正確保留或重置。
- 視專案測試架構補充輸入與狀態測試
  - 驗證方向鍵狀態、鏡頭模式循環及重啟後狀態重置。

## 4. Verification Plan

### 分段開發與驗證里程碑

核准後不會一次加入所有系統，而會依序完成下列里程碑。每一段都必須通過型別／建置檢查、該段自動化測試與瀏覽器冒煙測試，才進入下一段：

1. **專案骨架與追逐設定**：完成頁面骨架、場景／日夜／天氣／警車選擇與遊戲狀態模型。
2. **基礎駕駛**：完成道路、警車模型、方向鍵操作、加速、煞車、轉向與基本碰撞。
3. **嫌犯 AI**：完成道路追蹤、逃逸、速度控制、受阻反應與基本閃避。
4. **PIT 系統**：完成接觸區域、相對速度、碰撞角度、失控狀態及成功／失敗判定。
5. **鏡頭系統**：完成駕駛第一人稱、自動跟車及後方第三人稱視角和平滑切換。
6. **三種場景**：完成城市、鄉間與高速公路的道路及環境差異，驗證各場景均可完成追逐。
7. **環境系統**：完成日夜照明、晴雨雪效果及不同抓地力，驗證玩家和 AI 遵守相同規則。
8. **警車種類**：完成巡邏轎車、攔截跑車、警用 SUV 的外觀及性能差異。
9. **完整遊戲流程**：完成 HUD、倒數、暫停、勝負、重新開始、效能與響應式畫面整理。
10. **PWA 與正式發布**：完成安裝資訊、離線快取、更新策略、正式建置與公開網址部署。

### 最終驗證

所有里程碑完成後，將執行以下完整驗證：

1. 安裝依賴並執行正式建置，確認 TypeScript、模組解析及部署輸出沒有錯誤。
2. 執行自動化測試，覆蓋 PIT 判定、遊戲時限、勝負條件、輸入狀態與鏡頭切換邏輯。
3. 在背景啟動本地開發伺服器，使用瀏覽器實際進入遊戲。
4. 實際驗證：
   - 開始前可自由組合三種場景、日夜、三種天氣和三種警車，且選擇會正確帶入追逐。
   - 城市、鄉間、高速公路具有不同道路與環境外觀，三者都能完成一局追逐。
   - 白天／黑夜照明、車燈與 HUD 對比正常；雨雪粒子不遮蔽必要資訊。
   - 雨天比晴天更易打滑，雪天煞車距離最長；玩家與嫌犯均受到相同路面規則影響。
   - 三種警車的速度、操控與重量差異可感知，但任一車型皆可完成 PIT。
   - 方向鍵能控制警車，且頁面不會捲動。
   - 嫌犯車能自主沿道路逃逸並做出閃避反應。
   - 各視角可正常切換，第一人稱與跟車視角沒有明顯穿模或劇烈抖動。
   - 有效 PIT 能觸發嫌犯失控與成功結算，普通追撞不會誤判。
   - 暫停、倒數、失敗、成功與重新開始流程可重複操作。
   - 瀏覽器主控台無未處理錯誤，主要畫面在常見桌面尺寸下沒有遮擋。
5. 進行基本效能檢查，確認高頻更新不造成 React 每幀重繪，並觀察遊戲運行是否保持穩定、無持續增加的事件監聽器或資源洩漏。
6. 驗證 PWA 可由支援的桌面瀏覽器安裝、以獨立視窗啟動，已快取版本可離線進入，重新連線後可安全取得新版資源。
7. 完成正式部署後，由非 `localhost` 的公開網址再次執行主要流程驗證，確認其他電腦可直接存取。
8. 驗證結束後關閉本地伺服器，建立 `walkthrough.md` 記錄完成內容、效能處理、測試結果與正式遊戲網址。

## Approval Gate

目前只完成研究與規劃。未收到使用者明確回覆「同意」前，不初始化專案、不建立 `task.md`，也不修改或產生任何遊戲程式碼。
