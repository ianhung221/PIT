# PIT Unit V2 修正版 — Implementation Plan

## 1. Goal Description

依據玩家在 `v2_acceptance_test.md` 的實玩回報，在不破壞已通過功能、既有資料結構、重新挑戰流程與部署方式的前提下，分批修正 V2。

為降低錯誤率，修改拆成六批，每批完成測試與實玩驗證後才進入下一批：

1. 車模與座標基準。
2. 駕駛視角、快速後看、後視鏡與直升機鏡頭。
3. 道路完整性、多車道路載入、天候及行駛速度感。
4. 後援 AI 與無線電命令。
5. PIT 授權、碰撞分類與成功判定。
6. 漸進油門與可取消的巡航定速。

原本六批為本專案唯一正式修改順序；後續新增問題只能歸入既有批次或標示延後版本，不另立互相競爭的新順序。每一批完成驗證後，仍須取得玩家核准才進入下一批。

### 目前修改順序與進度（更新於 2026-09-22）

已完成：**Batch 3 — 玩家已於正式網站複驗，V2-FIX-007、008、010 及補修 V2-FIX-011 通過；009 保留原始回報**

目前進行：**Batch 4 — 已於 2026-09-22 核准，後援與六指令已通過本機驗證，等待正式發布及玩家複驗**

| 批次 | 狀態 | 目前範圍與問題歸屬 |
| --- | --- | --- |
| Batch 1 — 車模與座標基準 | ⚠️ 主要完成 | 車頭方向與官方材質已修正；跑車、SUV 的完整警用塗裝延至 V3。U-007 的精細碰撞邊界歸入 Batch 5，因為必須與 PIT 接觸區域一起校正。 |
| Batch 2 — 駕駛視角、後看、後視鏡與直升機 | ⭕ 完成 | V2-FIX-002～V2-FIX-004 與 V2-FIX-006 均經玩家複驗通過；完整迷你地圖延至 V3。新回報 U-012、U-013 不回溯混入已結案批次，批次歸屬列於下方待確認。 |
| Batch 3 — 道路完整性與天候 | ⭕ 完成 | V2-FIX-007、008、010 及補修 V2-FIX-011 均已由玩家確認通過；原 V2-FIX-009 的問題與回報保留為歷史紀錄。 |
| Batch 4 — 後援 AI 與無線電 | 🧪 等待玩家複驗 | V2-025、V2-026、V2-028 與 U-004 的循路追趕、撞牆脫困及六指令已通過本機測試；V2-027 的後援到位部分已驗證，PIT／包圍成功判定留 Batch 5。請依 V2-FIX-012～015 複驗。 |
| Batch 5 — PIT 授權、碰撞與成功判定 | ⬜ 未開始 | U-005 高速狀態的授權邏輯、U-007 車輛碰撞邊界、真實接觸區域、碰撞分類及成功條件。 |
| Batch 6 — 漸進油門、巡航定速與轉向手感 | ⬜ 未開始 | U-001 漸進油門與巡航定速、U-010 速度感應轉向及按鍵輸入平滑。 |

Batch 3 已通過玩家複驗並結案。Batch 4 已獲核准、完成實作及本機驗證；正式發布後請玩家依驗收表複驗。Batch 5 尚須另行規劃與核准。

### Research Findings

- `PROJECT_SOUL.md` 不存在，目前沒有可辨識的作品定位衝突。
- 正式 GLB 未套用程式中的車身顏色，且模型前方與物理引擎使用的本地 `-Z` 前方相反；這會同時造成車尾向前與 PIT 接觸位置的視覺誤判。
- 駕駛鏡頭用世界座標平滑追趕車輛，駕駛艙卻立即跟車；加減速時兩者產生相對位移，鏡頭因此穿過車體。
- `R` 後看的輸入已存在，但視線被不透明自車外殼遮住；後視鏡攝影機也位於車體中央，容易只拍到自車內部。
- 直升機鏡頭平均計算四台車的位置；後援落後或卡住時會把取景中心拖離玩家與嫌犯。
- 程序世界只以玩家為中心保留後方 3 段、前方 12 段道路。落後後援或超前嫌犯可能失去道路與護欄 collider。
- 跨區域道路直接改變寬度，缺少漸寬／漸窄的護欄接合件，因此交界會留下缺口。
- 嫌犯有卡牆恢復狀態機；後援只有簡單朝目標加速，沒有卡住偵測、倒車、轉正、合理初速或遠距追趕模式。
- 無線電 Q1、Q6 有明確效果；Q2–Q5 多數只有紀錄文字，部分命令沒有不同的 AI 目標。
- 兩台後援初速都是 0，但嫌犯以場景 AI 速度的 72% 起跑；後援又以玩家為主要跟車目標，因此開局落後或玩家沒有追上時，後援更難回到有效支援位置。
- `driveBodyToward` 只朝單一世界座標轉向及加速，沒有道路切線、路寬、前視導航點或卡住狀態；車頭朝向護欄後會持續把加速度施向錯誤方向，無法像嫌犯一樣經歷「煞停→倒車→轉正→重新加入」。
- 正常編隊與接替主追都直接以玩家／嫌犯車身附近的座標為目標。彎道或車距過大時，後援可能切向護欄，而不是先沿道路追回相同路段。
- `state.command` 目前只保存一個字串且沒有接收時間、執行狀態或完成條件。Q2「準備 PIT」與 Q3「第二單位靠近」都只回傳 `pit-ready`，但模擬器沒有依 `pit-ready` 改變任何目標；Q4「封鎖車頭」未被 `coordinatePursuit` 處理；Q5 會切換角色但沒有到位確認。
- Q2～Q5 的文字只寫入結算用 `eventLog`，追逐中沒有 `aria-live` 或 HUD 回覆，所以玩家無法判斷是沒有送出、尚未執行或已完成。
- 數字鍵按住時目前沒有排除鍵盤自動重複，可能在同一個指令上連續觸發；切換新戰術指令時也沒有一致的「新指令取代舊指令」規則。
- PIT 授權只在玩家提出請求時重算；速度警告可能與 `authorized` 同時出現，部分狀態在固定任務參數下無法自然出現。
- 一般碰撞雖有事件紀錄，遊戲中沒有即時分類提示。
- PIT 接觸區域使用兩車中心相對位置近似，不是真正碰撞面接觸點；配合顛倒車模，可能讓視覺上的車身中段被誤判為有效接觸。
- 雪點落下後全部回到相同高度，長時間後可能同步成看似完整的平面。
- 雨雪粒子群目前只跟隨玩家的位置，粒子本身只修改垂直座標；沒有保存每顆粒子的下降速度、重生相位，也沒有扣除玩家的水平速度，因此會同步成平面，且前進、後退時都像垂直落下。
- `ProceduralWorld.tsx` 目前只以玩家的 `roadIndex` 載入後方 3 段與前方 12 段。`V2Runtime` 雖保存玩家與嫌犯道路索引，卻沒有保存兩台後援的道路索引；嫌犯或後援與玩家拉開時，路面和護欄碰撞器可能先被卸載。
- 每段道路都用固定寬度的長方體路面及兩條獨立直線護欄。跨區域時寬度會由 11、15、19 公尺直接跳變，視覺路面雖有 2 公尺重疊，兩側護欄仍會留下可讓車輛穿入的斜向缺口。
- 道路中心線目前是每段 74 公尺左右的連續長條；缺少規律、可快速掠過的近距離標記，所以在長直路上不容易判斷速度。
- 現有道路測試只確認中心距離與基本 metadata，未驗證相鄰端面、路寬 transition、載入視窗或護欄覆蓋，因此這些問題能在測試全通過時仍出現。
- V2-FIX-009 的粒子初始 X、Y、Z 與下降速度都由粒子編號配合固定取餘數公式產生；各欄位看似分散但仍有數學相關性。雪花長時間循環後可能排列成斜向條帶，而目前測試只計算高度區間，無法偵測畫面中的斜向聚集。
- 雨雪使用未配置透明形狀的 `PointsMaterial`，每個點的原始輪廓是方形。三面後視鏡以低解析度 render target 重繪同一場景，再將結果放大到鏡面；靠近鏡頭的白色雪點與藍色雨點因此會成為醒目的大方塊。雪點尺寸 `.12` 大於雨點 `.045`，所以雪景更嚴重。
- 相同「起始區域＋seed」的道路已有自動測試；之後進入其他區域是綜合地圖的預期行為，V2-004 暫不視為已確認程式錯誤。
- C-002「放開 Q 自動送出」不符合目前確認式按鈕需求，暫不修改；C-003 射線避障待新取景實測後再決定。

## 2. Open Questions / User Review Required

以下原則已由玩家確認：

1. 遊戲維持 PIT 訓練模擬定位；玩家主追、AI 支援。
2. 繼續使用虛構車款、虛構單位與合法免費資產。
3. 保留後方追蹤；駕駛視角固定在車內；原自動跟車維持直升機視角。
4. 無線電使用預寫按鈕與數字鍵，不使用文字輸入。
5. Q 指令需經點擊或數字鍵確認，不採游標滑過後放開即送出。
6. 第一輪只修改 Batch 1、2，不順便更動 PIT、道路、後援 AI、天候或油門。
7. 第一輪暫採虛構黑白警用配色、紅藍警示燈及深紅嫌犯車；只覆寫車身，不改輪胎、玻璃與燈具。

### Batch 3 已核准範圍（2026-09-16）

1. **U-012 駕駛視角下方橫條**：這是 Batch 2 結案後新增的車內畫面微調，不影響 Batch 3 的道路或天候。建議延至 V3 的車內視覺完善，屆時會先判斷它是儀表板、擋風玻璃下緣或其他模型，再決定降低或移除，不在本批順手修改。
2. **U-013 後看時的鏡面與前方小視窗**：隱藏後視鏡是既有畫面的微調，但角落前方視窗等於增加第四個即時攝影機，會影響效能與低／中／高畫質規則。建議整項延至 V3 視角／畫面批次統一設計；Batch 3 不修改。
3. **道路標線**：建議採符合場景的標線，而不是所有道路只放同一條虛線：城市與鄉間使用中央分隔標線，高速公路使用車道虛線；標線只改善速度感，不在本批增加交通規則或車道 AI。

以上方案隨 Batch 3 一併核准；U-012、U-013 留待 V3 視覺／視角批次。

### V2-FIX-009 天候補修（2026-09-21 已核准執行）

玩家已確認雨雪相對車速移動正確，補修只處理以下兩項：

1. 局末雪花不得形成斜向條帶；保留前進、倒車、側滑及靜止時目前正確的相對移動。
2. 中央與左右後視鏡保留雨雪效果，但粒子改為細小、柔邊、半透明，不遮住道路與車輛；不直接關閉鏡中的天候。

本次不加入擋風玻璃水滴、雨刷、積雪、風向系統，也不修改道路、鏡頭位置、後援 AI、PIT 或操控。沒有需要玩家另選的設計問題。

### Batch 4 行為（2026-09-22 已核准）

本批沿用已確認的「玩家主追、AI 輔助」定位。後援不會自行執行 PIT，也不會因追趕而獲得不合理瞬移或穿牆能力。

1. **Q1 請求 PIT 授權**：維持現有風險評估；顯示指揮中心已接收及評估結果。本批不改授權門檻，U-005 仍屬 Batch 5。
2. **Q2 準備執行 PIT**：01 保持主追；02 縮短到近距支援位，03 保持較安全的後方支援位。這項命令也可結束 Q5 的接替狀態，讓玩家重新成為主追。
3. **Q3 第二單位靠近**：02 沿道路移到玩家與嫌犯之間的近距支援位；到位後明確回覆，03 仍維持第三順位。
4. **Q4 封鎖嫌犯車頭**：只有嫌犯已因合格 PIT／碰撞明顯減速時，02 才前往車頭封鎖；高速追逐中回覆「條件不足，等待嫌犯減速」，不做危險超車。此處只負責後援到位，不提前改寫 Batch 5 的 PIT 或 contained 判定。
5. **Q5 接替主追單位**：02 沿道路移到嫌犯後方並成為 primary，01 顯示為 secondary；持續到 Q2、任務重開或結束，不因一幀距離變化反覆切換。
6. **Q6 終止追逐**：維持立即結束與寫入報告；增加清楚的已執行回覆。

Q2～Q5 採「後送出的戰術命令取代前一個」；Q1 是獨立的授權詢問，不會意外取消正在執行的接替或編隊命令。以上是依既有手冊語意整理的建議方案，目前沒有必須由玩家另選的視覺或操作問題。

## 3. Proposed Changes

### Batch 1 — 車模與座標基準（第一輪）

#### `components/game/VehicleModel.tsx`

- 為每種 GLB 建立視覺座標轉接，統一模型車頭與物理本地 `-Z` 前方。
- 只旋轉視覺模型，不更改剛體、碰撞盒、轉向或 AI 導航。
- 複製每台車的材質，避免共享 GLTF 材質造成互相改色。
- 依 mesh／material 名稱區分車身、玻璃、車輪及燈具，只覆寫車身配色。
- 校正警燈、車號牌與附加物位置；保留 GLB 失敗時的 fallback。
- 支援駕駛模式下只對主攝影機隱藏玩家外部車殼，不隱藏剛體與 collider。

#### `lib/gameConfig.ts`

- 集中保存各模型的視覺旋轉、位移、縮放與配色。
- 保持既有 `VehicleId`、物理尺寸、質量及性能資料相容。

#### Tests

- 驗證所有車型都有合法朝向、縮放與配色設定。
- 驗證視覺旋轉不改變物理前方與既有左右轉向。
- 驗證警車與嫌犯配色不同，材質設定不共用可變物件。

### Batch 2 — 駕駛視角、後看、後視鏡與直升機（第一輪）

#### `components/game/CameraRig.tsx`

- 駕駛鏡頭改用車輛本地座標固定錨點，不再用會產生明顯位移落差的世界座標追趕。
- 只對旋轉或微小震動做受限平滑。
- R 後看使用明確後方姿態，放開後平滑回前視。
- 直升機以玩家與嫌犯為主要加權中心，只有合理距離內的後援影響高度與取景。
- 第一輪不加入建築射線避讓，先驗證新取景是否已足夠。

#### `components/game/CockpitView.tsx`

- 駕駛艙與鏡頭共用同一套本地座標。
- 駕駛模式隱藏玩家不透明外殼，使用通用儀表板、方向盤與 A 柱提供車內參考。
- 前方不放置不透明擋風玻璃；後看時隱藏會阻擋視線的駕駛艙部分。
- 保留方向盤依左右輸入旋轉。

#### `components/game/VehicleMirrors.tsx`

- 將中央及左右鏡攝影機移到各自實際位置。
- 使用 layers 或等效方式排除玩家自己的外部車殼。
- 固定鏡面 render order，避免被儀表板或車殼遮擋。
- 保留低畫質只有中央鏡、中高畫質提供三面鏡及降頻渲染。

#### `components/game/PitGameV2.tsx`

- 把視角模式傳給玩家車模，以控制主攝影機下的自車外殼可見性。
- 保留 `C` 切換、HUD、暫停、重新挑戰與返回設定流程。

### Batch 3 — 道路完整性、多車道路載入、天候及速度感（2026-09-16 已核准）

#### `types/game.ts`

- 在不改變現有欄位語意的前提下，為道路段加入 transition 所需的起點／終點寬度資料；舊有 `width` 仍保留作為該段代表寬度，避免破壞 AI、HUD 與既有測試。

#### `lib/proceduralMap.ts`／`lib/roadModules.ts`

- 將跨區域的直線 transition 改為由前一段寬度平滑收放至下一段寬度，並讓路面、邊線、護欄共用同一組端面資料。
- 保留相同 seed 的決定性；若輸出幾何必須改變，在地圖代碼或生成資料中加入明確版本，讓日後能辨認新舊生成規則。
- 抽出「多車可見道路索引聯集」純函式：以玩家、嫌犯和兩台後援所在路段建立緩衝區段，合併重疊範圍並設定總量上限，避免無限制增加 Three.js mesh 與 Rapier collider。
- 新增相鄰道路端面連續性、transition 寬度及多車載入範圍測試。

#### `components/game/v2Runtime.ts`／`components/game/V2VehicleSimulation.tsx`

- 為兩台後援保存各自的道路索引，沿用現有 `closestRoadSegment` 更新方式。
- 只增加世界載入需要的索引資料，不改後援駕駛、追趕、卡牆恢復或無線電行為；這些仍屬 Batch 4。

#### `components/game/PitGameV2.tsx`

- 改傳玩家、嫌犯及兩台後援的道路索引給程序世界，不再以低頻 HUD 中單一玩家索引作為唯一載入依據。
- 保留目前所有視角、HUD、暫停、重新挑戰、輸入與 PIT 流程。

#### `components/game/ProceduralWorld.tsx`

- 依多車載入索引聯集渲染道路、地面、造景及碰撞器，確保玩家落後或車隊分散時，每台車附近仍有完整路面與護欄。
- transition 使用連續的收放路面與護欄接合幾何，封閉 V2-006 的跨區域洞口及 V2-007 的護欄接縫；視覺與 collider 必須採同一邊界。
- 將長條中心線改為依場景配置的短節距標線，重複物件採合併幾何或 instancing，避免為速度感大量增加 draw call。
- 不在本批改造城市、鄉村、高速公路的造景美術，也不移除未來可替換為場景碰撞物的道路邊界。

#### `lib/weatherMotion.ts`（新增）／`components/game/V2Weather.tsx`

- 抽出可測試的粒子初始化、重生及速度計算；每顆雨滴／雪花使用不同相位、下降速度與重生高度，消除長時間同步成平面。
- 依玩家速度與朝向計算相對水平漂移：前進時粒子迎向玩家、倒車時方向反轉、靜止時以垂直下降為主。
- 每幀只更新既有 `Float32Array` 與 `BufferAttribute`，不建立 React state、不在迴圈內新增 DOM／Three.js 物件。
- 對過大的 `dt` 設上限並使用分散式重生，避免分頁切回或掉幀時粒子瞬間聚成一層。

#### Tests

- 相同 seed 與起始區域仍產生相同道路；不同 seed 仍具有變化。
- 三種跨區域寬度組合的路面端面與護欄連續，不留下大於碰撞容差的孔洞。
- 玩家、嫌犯或任一後援相距很遠時，各自前後安全緩衝都會載入，且總區段數維持上限。
- 雨雪粒子經長時間模擬後高度不會全部相同；前進與倒車的相對漂移方向相反；大 `dt` 後仍保持分散。

### Batch 3 天候補修 — V2-FIX-009（2026-09-21 已核准範圍）

#### `lib/weatherMotion.ts`

- 將以粒子編號直接取餘數的初始化改為固定 seed 的雜湊亂數；X、Y、Z、下降速度、水平飄動相位使用彼此獨立的亂數通道。
- 保存每顆雪花的飄動相位與幅度，以小幅、不同步的水平位移打散長時間循環軌跡；雨滴維持較直的落下路徑。
- 粒子越過下界時使用各自的重生高度與水平偏移重新進入，保留超出量，避免突然跳動或再度同步。
- 保留現在已驗收通過的玩家實際位移補償及 `dt` 上限。

#### `components/game/V2Weather.tsx`

- 使用可重用的柔邊圓形透明遮罩，使雪花與雨點不再呈現正方形；遮罩在元件生命週期內建立一次並在卸載時釋放。
- 分別設定雨與雪的主畫面尺寸、透明度與深度行為，維持可見性但降低近距離遮擋。
- 為雨雪物件及材質加入明確名稱，讓後視鏡渲染可以安全辨識並暫時套用鏡面尺寸。

#### `components/game/VehicleMirrors.tsx`

- 每次渲染中央／左右鏡前，暫時把雨雪點尺寸與透明度降為鏡面專用值；完成所有鏡面後立刻恢復主畫面材質。
- 使用 `try/finally` 恢復 render target、駕駛艙、雨雪材質及鏡面顯示狀態，避免渲染中斷後污染主畫面。
- 保留目前鏡面解析度、更新頻率、左右翻轉及低畫質只顯示中央鏡的規則。

#### `tests/world-weather.test.mjs`

- 保留現有高度分散與車速相對移動測試。
- 新增固定 seed 可重現、各亂數通道不重複及雨雪長時間網格分布測試。
- 在 90 秒、180 秒及大 `dt` 後檢查粒子不能集中成少數水平帶或斜向帶；測試指標採二維格網占用率與對角線集中度，不只計算 Y 高度。

#### `scripts/browser-smoke.mjs`

- 延長降雪實玩到倒數最後十秒，保存主畫面及三面鏡截圖。
- 新增鏡面 render target 的像素／畫面檢查，確認沒有單顆大型純白或純藍方塊長時間遮住鏡面。
- 雨、雪各測中／高畫質駕駛視角；低畫質確認中央鏡與既有畫質規則不回歸。

### Batch 4 — 後援 AI 與無線電（2026-09-22 已核准範圍）

#### `types/game.ts`

- 擴充後援 AI 模式、戰術指令狀態與回覆階段型別；保留現有 `RadioCommand`、`PursuitRole` 與任務結果值，避免破壞既有資料結構。

#### `components/game/v2Runtime.ts`

- 為 02、03 各保存卡住秒數、脫困階段、階段時間及上次有效進展，不與嫌犯共用可變狀態。
- 將「最後收到的按鈕」與「持續生效的戰術命令」分開；保存回覆文字、狀態、發令時間及到位資訊。
- 後援起始速度調整為符合開局車隊速度，但不瞬移、不提高物理極速。

#### 新增 `lib/supportUnitAI.ts`

- 以純函式計算後援的循路導航點、角色隊形、遠距追趕速度及到位條件。
- 卡住判斷同時檢查低速持續時間、道路橫向位置、朝向誤差及目標進展，避免把正常減速誤判為卡牆。
- 建立「煞停→倒車離障→朝道路切線轉正→重新加速」狀態機；雨雪抓地力會影響加減速，但不改變階段順序。
- 遠距 catch-up 只在道路上提高目標速度至車型既有限制附近，不使用 teleport、穿牆或直接設定位置。

#### `lib/pursuitCoordinator.ts`

- 將 Q2～Q5 對應為不同且持續的角色與隊形意圖；Q1 不覆蓋戰術命令。
- 提供各指令的合法條件、執行中及完成判斷，尤其 Q4 只有在嫌犯明顯減速後才允許前封。
- 保留玩家預設 primary，且後援永遠不自行發動 PIT。

#### `components/game/V2VehicleSimulation.tsx`

- 將兩台後援的直接世界座標追逐改為「道路前視導航＋最終隊形偏移」；彎道先沿道路方向移動，再接近玩家或嫌犯旁的角色位置。
- 對兩台後援分別套用脫困控制、道路邊界修正、合理追趕加速度與角色目標，避免兩車共用同一位置互撞。
- Q2 為 PIT 支援隊形、Q3 讓 02 靠近、Q4 在安全條件成立後前封、Q5 讓 02 接替嫌犯後方主追；每項依距離、朝向和速度判斷「執行中／完成／無法執行」。
- 有效 PIT 後仍讓 02、03 前往車頭與車尾支援，但不更改現有碰撞分類、PIT 合格門檻、包圍成功距離或嫌犯再次逃逸條件；這些維持 Batch 5 範圍。

#### `components/game/PitGameV2.tsx`

- Q1 繼續更新授權區；Q2～Q6 顯示即時、短暫且可讀的派遣回覆，區分「已接收、執行中、完成、無法執行」。
- 將 01／02／03 的英文內部角色轉為繁體中文 HUD 標籤，並顯示目前有效戰術命令；不每幀建立大量 React 狀態。
- 更新行動後報告，使實際送出與完成／拒絕的命令都有紀錄。

#### `components/game/RadioCommandWheel.tsx`／`app/globals.css`

- 保留滑鼠按鈕與 Q＋數字鍵操作，不增加文字輸入或放開 Q 自動送出。
- 忽略鍵盤自動重複，送出後明確關閉圓盤；加入可存取的 `aria-live` 回覆樣式與狀態顏色。

#### `tests/v2-rules.test.mjs`／新增後援 AI 測試

- 覆蓋兩台後援的正常跟車、遠距追趕、彎道道路導引、卡住誤判防護及完整脫困狀態轉移。
- 驗證 Q1 不取消戰術指令，Q2～Q5 行為互異、新指令取代舊指令、Q4 高速時拒絕／低速時執行、Q5 穩定接替。
- 保留並擴充既有協調、道路、PIT、鏡頭及雨雪測試，不刪除或放寬舊斷言。

#### `scripts/browser-smoke.mjs`

- 加入可重現的後援落後、02／03 撞牆及六項無線電命令情境；只在測試頁面控制剛體，不改正式遊戲初始狀態。
- 擷取直升機、駕駛後視鏡、指令回覆與後援脫困後重新追上的畫面，並檢查主控台及資源錯誤。

#### `GAME_MANUAL.md`／`v2_acceptance_test.md`／`task.md`／`walkthrough.md`

- 執行獲准後才建立 Batch 4 任務清單，並更新六項命令的實際持續時間、取代規則與安全限制。
- 完成後在 D 區新增 Batch 4 修正驗收列，不覆蓋 A～C 區或玩家既有回報；Walkthrough 記錄測試、效能及發布結果。

### Batch 5 — PIT 授權、碰撞與成功判定（後續獨立核准）

- `pitPolicy.ts`：讓狀態與原因一致；授權加入有效期限與重新評估條件；建立可重現的 authorized／hold／denied／terminate 情境。
- `V2VehicleSimulation.tsx`／`pitSimulation.ts`／`gameRules.ts`：以 Rapier 接觸資料或 OBB 最近接觸投影取得實際接觸區域。
- 有效候選必須同時符合授權、後四分之一、角度、閉合速度、接觸持續及後續旋轉／失控門檻。
- 一般擦撞、未授權與過度撞擊不增加有效 PIT，但仍造成合理車損。
- `PitGameV2.tsx`：增加即時碰撞分類；成功後仍依車損與包圍結果決定停駛、受控或再次逃逸。

### Batch 6 — 漸進油門與巡航定速（後續獨立核准）

- `V2VehicleSimulation.tsx`／`v2Runtime.ts`：加入逐步升降的虛擬油門與巡航目標速度。
- 煞車、倒車、明顯手動接管、暫停或任務結束立即取消巡航。
- 巡航只控制縱向速度，不代替玩家轉向或執行 PIT。
- `PitGameV2.tsx`／`globals.css`：HUD 顯示油門與巡航狀態，避免以高頻 React state 驅動。

### 驗收紀錄

- `v2_acceptance_test.md` 的 A、B、C 區既有結果永久保留。
- 每完成一批，在 D 區新增 `V2-FIX-xxx`，記錄日期、內容、驗證方式與預期結果。
- 不把舊 ❌／⚠️ 擅自改成 ⭕；複驗結果記在新增項目。
- 後續以 Markdown 為唯一人工回報來源，不再要求填寫 Excel。

## 4. Verification Plan

### 每批固定門檻

1. 修改前先補可抽離規則的防回歸測試。
2. 執行 `npm run lint`。
3. 執行 `npm test`；正式 build 與既有測試必須通過，不得刪除或放寬舊斷言。
4. 啟動背景本地伺服器，確認無編譯、執行期或未處理 Promise 錯誤。
5. 使用內建瀏覽器實玩；若沙盒仍不可用，明確標記未完成，由玩家依 `v2_acceptance_test.md` 回填。
6. 完成後關閉本地伺服器；本批通過後才提出下一批核准。

### Batch 1 驗證

- 所有警車及嫌犯均車頭朝行進方向；前進、煞車、倒車與左右轉向保持正確。
- 車模與碰撞盒大致重合。
- 警車與嫌犯配色可辨識；輪胎、玻璃、燈具不被誤染；不同實例不互相改色。
- GLB 載入失敗時 fallback 仍可完成遊戲。

### Batch 2 驗證

- 各車型在加速、等速、煞車、倒車及轉彎時，駕駛鏡頭不穿過車體，前方不被白色車殼遮擋。
- 按住 R 可見後方，放開平滑回正；失焦不會卡在後看。
- 中央鏡在三種畫質正常；側鏡依畫質規則啟用，且不顯示自車白色後座。
- 後方追蹤不回歸；`C` 切換與 HUD 正確。
- 故意讓後援落後時，直升機仍以玩家與嫌犯為中心。

### Batch 3 驗證

- 執行既有測試及新加入的道路端面、載入範圍、粒子分散與相對速度純函式測試。
- 使用城市、鄉間、高速公路三種起始區域及至少三組 seed，開過兩次區域交界；路面、邊線和護欄必須連續，沿兩側行駛不會穿出或被接縫彈飛。
- 開局故意落後至少 30 秒，以直升機視角觀察嫌犯；另外觀察兩台落後後援。四台車周圍均應持續有路面、護欄與碰撞，不得掉入空白處。
- 晴天直路及高速公路實玩，確認場景對應的短節距標線能提供清楚速度感，且沒有明顯閃爍或大量掉幀。
- 雪景連續運行至本局後半段，確認雪花保持分散；雨、雪各測前進、靜止及倒車，相對移動方向符合車速。
- 在低、中、高畫質各做一次短測，並回歸後方追蹤、駕駛、直升機、R 後看與三面後視鏡，確保 Batch 2 不退步。
- 啟動本地伺服器並使用內建瀏覽器實際操作；驗證完成後關閉伺服器。完成本機驗證及文件紀錄後，依玩家既有授權發布 GitHub Pages，再對正式網址做一次基本實玩。

### V2-FIX-009 補修驗證

- 執行 `npm run lint`、`npm test` 與 `npm run build:pages`；不得刪除或放寬既有 30 項測試。
- 降雪場景從開始持續至倒數 10 秒內，分別觀察後方追蹤、駕駛與直升機視角；雪花保持隨機分散，不形成水平面或斜向條帶。
- 駕駛視角在雨、雪各測前進、停止及倒車，確認相對速度方向仍與玩家已通過的版本一致。
- 中、高畫質分別檢查中央、左、右後視鏡；低畫質檢查中央鏡。粒子應為小型柔邊點，不出現大型白色／藍色方塊，不遮住後援與道路。
- 以瀏覽器截圖比較主畫面與鏡面粒子尺寸；檢查執行期錯誤、資源載入失敗及長時間效能。
- 關閉本地伺服器，更新 `v2_acceptance_test.md`、`task.md` 與 `walkthrough.md`，沿用既有授權發布 GitHub Pages，再驗證正式網址。

### Batch 4 驗證

- 執行 `npm run lint`、`npm test` 與 `npm run build:pages`；既有 32 項測試全部保留，新增後援 AI、協調與命令狀態測試。
- 晴天、雨天、雪天各測至少一局；讓兩台後援分別正面卡住左右護欄，確認它們會煞停、倒車、轉回道路並重新加入追逐，且正常低速跟車不會無故倒車。
- 讓玩家與嫌犯拉開後援至少數個道路區段，確認 02、03 沿已載入道路追回，不切過護欄、不瞬移、不停在原地，並能恢復預定隊形。
- 逐一用滑鼠及 Q＋1～6 送出命令：每項都要立即顯示已接收，之後顯示執行中、完成或具體無法執行原因；按住數字鍵不重複送出。
- Q2、Q3、Q5 必須產生肉眼可辨識且不同的隊形；Q4 在高速時不得危險超車，嫌犯減速後才能前封；Q1 不取消目前隊形；Q6 正常結束並寫入報告。
- 以直升機確認整體隊形，並用駕駛視角與後視鏡確認後援接近時不長時間撞擊玩家；回歸 C、R、P、道路、天候、重新挑戰及返回設定。
- 驗證完成後關閉本機伺服器，更新永久紀錄；依既有發布授權推送 GitHub Pages，再對正式網址重跑六指令及至少一個撞牆脫困情境。

### Batch 5–6 驗證摘要

- Batch 5：四種授權可重現且原因一致；錯誤接觸不增加 PIT；合格接觸仍須達失控門檻。
- Batch 6：虛擬油門連續變化；巡航可維持速度，並被煞車、倒車、接管、暫停與結束可靠取消。

### Walkthrough 與發布

- 完成每次核准範圍後更新 `walkthrough.md`，記錄修改、效能影響、自動測試、實玩及未完成項目。
- 玩家已授權後續完成且驗證通過的修改可直接發布；目前正式發布目標為既有 GitHub Pages，不改回 Sites，也不建立新 `project_id`。
- 發布後重新驗證正式網址；若正式站失敗，保留上一個可用版本並在 Walkthrough 明確記錄。

## Approval Gate

玩家已於 2026-09-22 回覆「同意執行第四批」。依核准範圍實作後援與六指令、建立測試、更新文件並完成驗證後發布；玩家複驗通過後才結案。Batch 5、Batch 6、U-012 與 U-013 不在本次範圍，下一批仍須先規劃並獲得核准。

---

# （已取消）學校帳號 Sites 遷移 — Implementation Plan

> 狀態：此方案不執行。因學校帳號日後也會被刪除，現改採下方的 GitHub Pages 遷移方案；本段僅保留作為決策紀錄。

## 1. Goal Description

讓目前登入的學校 Codex 帳號可以持續編輯、建立版本與發布 PIT Unit，同時：

- 完整保留目前本機程式碼、Git 歷史、遊戲資料結構與既有功能。
- 不刪除、覆蓋或停用私人帳號擁有的舊 Site。
- 以相同程式碼在學校帳號下建立新的 Sites 專案，因此會取得新網址。
- 新站驗證成功前，舊站繼續正常運作，作為可回復版本。
- 後續已核准的程式修改在本機驗證完成後，可直接發布到學校帳號的新 Site；修改程式前仍必須先取得玩家核准。

### Research Findings

- 本機工作目錄乾淨，最新 Git 提交包含 V2 第一輪修正及 Sites version 5 發布紀錄。
- 現有 .openai/hosting.json 指向私人帳號的 Site：appgprj_6a85b0a3b18081918944d43f2248899b。
- 學校帳號的 Sites 擁有／可編輯清單皆為空，且查詢上述專案得到 project_not_found；因此學校帳號無法更新原 Site。
- 本機專案不綁 ChatGPT 帳號，學校帳號仍可編輯、測試和提交同一份程式碼。
- 現有 Vinext 建置與 Sites 輸出格式已曾成功發布，不需要為帳號遷移改寫遊戲或更換框架。
- PROJECT_SOUL.md 不存在，沒有可辨識的作品定位衝突。

## 2. Open Questions / User Review Required

### 已採用的建議預設

1. 新 Site 名稱仍使用「PIT Unit」。
2. 新網址的 slug 優先嘗試 pit-unit-pursuit；若已被舊站占用，使用平台提供的最接近可用名稱，不為取得相同網址刪除舊站。
3. 新站完成驗證後，以「任何取得網址的人都能遊玩」為目標發布，延續目前的使用方式。
4. 若學校管理員禁止公開 Sites，立即停止公開發布，不擅自改成只限全校或邀請其他人；保留已建立的私人新站並回報，再另行規劃 GitHub Pages 等免費替代方案。
5. 舊 Site 的專案 ID、正式網址與最後版本會先寫入永久遷移紀錄，才更換本機 manifest。

### 需要玩家核准的影響

- 新 Site 必然使用新網址；舊網址仍由私人帳號管理，無法轉移給學校帳號。
- .openai/hosting.json 的 project_id 會在新 Site 建立成功後改成學校帳號的新專案 ID。這只改變後續發布目標，不會改變遊戲內容。
- 執行階段會建立新網站並正式發布，屬外部狀態變更；必須收到本計畫的明確核准才執行。

目前沒有其他設計問題需要決定。如不同意上述公開範圍或命名方式，請在核准前指定。

## 3. Proposed Changes

### deployment_targets.md（新增）

- 建立永久部署目標紀錄，不覆蓋舊資料。
- 記錄私人帳號舊 Site 的名稱、專案 ID、網址、最後已知版本與狀態。
- 新 Site 建立後追加學校帳號的新專案 ID、網址、建立日期與目前主要發布目標。
- 不記錄登入資訊、權杖或其他秘密。

### .openai/hosting.json

- 新 Site 尚未成功建立前不修改。
- 建立成功並取得確切新 project_id 後，只替換 project_id。
- 保留現有 d1: null、r2: null 與其他合法設定，不加入帳號資料或憑證。
- 不再指向私人帳號舊 Site；需要回查時以 deployment_targets.md 的紀錄為準。

### task.md

- 新增「學校帳號 Sites 遷移」工作清單。
- 逐項記錄舊站備份、新站建立、manifest 更新、建置、版本儲存、公開發布、正式網址驗證及文件更新。

### walkthrough.md

- 追加遷移紀錄，不覆蓋先前版本內容。
- 說明舊站仍保留、新站網址、存取範圍、驗證結果，以及後續發布改由學校帳號管理。

### Sites 平台操作

1. 再次確認學校帳號下沒有尚未完成或重複的 PIT Unit Site。
2. 使用學校帳號建立一次新的 Sites 專案；不得重複建立。
3. 立即把平台回傳的新 project_id 寫入 .openai/hosting.json，並核對無衝突。
4. 以目前 Git HEAD 建置與封裝相同程式碼。
5. 將確切來源提交推送到新 Site 的來源倉庫。
6. 儲存新版本並依學校帳號允許的權限發布。
7. 新站成功前不變更或刪除舊站。

### 遊戲程式與資料

- 不修改 components/、lib/、tests/、遊戲資產、規則、物理或 UI。
- 不新增資料庫或雲端儲存。
- 不變更舊 Site 的存取權限、slug、版本或內容。

## 4. Verification Plan

### 遷移前

1. 確認 Git 工作目錄乾淨，記錄目前完整 HEAD。
2. 執行 npm test，確認正式建置及既有 24 項測試仍通過。
3. 確認舊網址仍能正常回應，但不使用學校帳號修改它。

### 新 Site 建立與發布

1. 確認新 Site 回傳的 project_id 與舊 ID 不同。
2. 重新讀取 .openai/hosting.json，確認只切換發布目標。
3. 確認新 Site 中的來源 commit 與本機完整 HEAD 一致。
4. 檢查封裝檔含 dist/server/index.js、dist/client/ 與 dist/.openai/hosting.json。
5. 等待部署狀態為 succeeded；失敗時保留已儲存版本，不重複建立網站。

### 正式網址驗證

1. 新網址回傳成功，不是錯誤頁或登入循環。
2. 首頁能選擇場景、時段、天候、警車與畫質。
3. 能進入遊戲，車模材質與方向正確。
4. 方向鍵、C 視角切換、R 快速後看及 Q 無線電仍能運作。
5. 確認新站的實際分享範圍；若學校政策不允許公開，停止並回報。
6. 驗證後關閉本機伺服器，更新 walkthrough.md。

## Approval Gate

目前只完成研究與更新 implementation_plan.md，尚未建立新 Site，也未修改發布目標。

收到玩家明確回覆「同意執行學校帳號 Sites 遷移」前：

- 不建立或更新 task.md、deployment_targets.md、walkthrough.md。
- 不修改 .openai/hosting.json。
- 不建立、發布或調整任何 Site。
- 不修改遊戲程式、測試、資產或資料結構。

---

# GitHub Pages 永久發布遷移 — Implementation Plan

## 1. Goal Description

將 PIT Unit 從綁定特定 Codex／Sites 帳號的發布方式，遷移到玩家長期持有的個人 GitHub 帳號與 GitHub Pages：

- 保留本機程式碼、Git 歷史、遊戲功能、測試與資料結構。
- 使用獨立的「專案網站」儲存庫與網址，不和玩家另一個專案互相覆蓋。
- 維持 Vinext 本機開發流程，另外加入適合 GitHub Pages 子路徑的靜態輸出。
- 保留現有 Sites 網站作為舊版備援，不刪除、不覆蓋。
- 後續核准的版本可經 GitHub Actions 自動測試、建置及發布。

### Research Findings

- 本機分支為 main，已有完整提交歷史，但目前沒有 Git remote。
- .openai/hosting.json 仍指向私人帳號的舊 Sites 專案；GitHub Pages 不使用它，因此不需刪除或改寫。
- Vinext 1.0.0-beta.2 支援 output: export、basePath 與 assetPrefix，靜態輸出為 dist/client。
- 遊戲頁面可做客戶端靜態發布；但 app/layout.tsx 使用 next/headers 動態產生 metadata，會阻擋純靜態匯出。
- 車模、PWA、manifest、service worker、favicon 與 Open Graph 圖片目前含根目錄絕對路徑。GitHub Pages 專案網站位於「/儲存庫名稱/」子路徑，直接發布會造成資源 404。
- public 已包含所需 GLB 與靜態資產，不需重新下載美術資源。
- 本機未安裝 gh CLI；建立遠端儲存庫時，需透過瀏覽器、GitHub 連接器或玩家手動操作授權。
- GitHub Pages 專案網址為 https://帳號.github.io/儲存庫名稱/。不同儲存庫使用不同子路徑，另一個專案不會與 PIT Unit 衝突。
- 本機同時執行兩個專案時，只有強制使用相同連接埠才會衝突；第二個專案可使用 3001 等其他可用連接埠。
- GitHub Free 的 Pages 零預算方案原則上需使用公開儲存庫；私人儲存庫 Pages 取決於付費方案。
- PROJECT_SOUL.md 不存在，沒有可辨識的作品定位衝突。

官方參考：

- GitHub Pages：https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages
- 自訂 Actions：https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
- 發布來源：https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site

## 2. Open Questions / User Review Required

### 必須由玩家確認

1. **是否接受原始碼公開？**
   建議接受，建立公開儲存庫，以符合零預算 GitHub Pages 方案。執行前會再掃描秘密資料。若不接受，需改用付費私人 Pages 或另選可連接私人儲存庫的免費代管服務。
2. **長期個人 GitHub 帳號名稱。**
   不能使用日後會被刪除的學校帳號；執行時若未登入指定帳號，會停在授權步驟。
3. **儲存庫名稱。**
   建議使用 PIT；若該名稱已存在則使用 pit-unit。最終網址與 base path 都會包含確定後的名稱。

### 已採用的建議預設

- 使用 GitHub「專案網站」，不占用唯一的 帳號.github.io 使用者網站。
- 保留現有 Sites 網站在線作為備援。
- GitHub Pages 成功上線後，將其記錄為主要公開網址。
- 本機不鎖死連接埠；3000 被占用時使用 3001 或自動選擇的可用連接埠。
- 不改遊戲玩法、物理、AI、視角、畫面或資料結構。

## 3. Proposed Changes

### next.config.ts（新增）

- 只在 GitHub Pages 建置環境啟用 output: export。
- 依核准後的儲存庫名稱設定 basePath、assetPrefix 與靜態網站尾斜線；本機開發維持空路徑。
- 不移除 Vinext 或現有 Sites 建置能力。

### app/layout.tsx

- 移除靜態匯出不支援的 next/headers 動態依賴。
- 使用建置時環境資訊產生 metadata、manifest、favicon 與 Open Graph 路徑。
- 保留目前標題、描述、PWA 與分享資訊。

### lib/publicPath.ts（新增）

- 集中產生可感知 base path 的公開資源路徑。
- 本機與 Sites 維持根路徑；GitHub Pages 自動加入儲存庫子路徑。

### components/game/VehicleModel.tsx

- 四個 GLB 路徑改用共用公開路徑函式。
- 不改模型、材質、碰撞盒、性能或 fallback。

### components/PwaRegister.tsx、public/manifest.webmanifest、public/sw.js

- 以正確專案子路徑註冊 service worker。
- manifest 的 start_url、scope、圖示改成可由專案子路徑解析。
- service worker 由自身 scope 建立快取網址，離線 fallback 回專案首頁。
- 更新快取版本，避免舊根路徑快取污染新站。

### package.json、scripts/verify-static-export.mjs

- 新增 GitHub Pages 專用建置與輸出驗證指令。
- 驗證 dist/client 的首頁、manifest、service worker、favicon 與必要 GLB 完整存在。
- 掃描關鍵資源，避免仍錯指向 GitHub 帳號根目錄。
- 保留既有 dev、build、test、lint 指令。

### .github/workflows/deploy-pages.yml（新增）

- 使用 GitHub 官方 Pages Actions；推送 main 後自動安裝、測試、靜態建置、上傳 dist/client 並部署。
- 只授予部署所需最小權限，不儲存密碼或個人權杖。
- 將實際儲存庫名稱與正式網址以環境變數傳給建置。
- 匯出內容包含 .nojekyll。

### deployment_targets.md（新增）

- 永久追加舊 Sites 與 GitHub Pages 的專案 ID／網址、日期及主要／備援狀態。
- 不保存登入資料或權杖。

### README.md

- 加入 Pages 網址、Actions 發布流程與多專案連接埠說明。
- 保留舊 Sites 網址並標為備援。

### task.md、walkthrough.md

- 收到執行核准後才建立／更新 task.md。
- 完成後在 walkthrough.md 追加正式網址、自動化與驗證結果，不覆蓋舊紀錄。

### GitHub 外部操作

1. 確認登入的是指定的長期個人 GitHub 帳號。
2. 掃描秘密與大型檔案；若有不應公開內容，停止並回報。
3. 檢查同名儲存庫；不得覆蓋其他專案。
4. 建立一次新的公開儲存庫，設定 origin，推送完整 Git 歷史。
5. 將 Pages Source 設為 GitHub Actions，等待首次部署後驗證正式網址。

### 明確不修改

- 不修改遊戲規則、物理、AI、視角、車模內容或使用者資料。
- 不刪除舊 Sites 專案或 .openai/hosting.json。
- 不更動玩家另一個 GitHub 專案、儲存庫、remote 或 Pages 網址。
- 不建立 GitHub 帳號、不保存密碼、不將專案放進學校管理帳號。

## 4. Verification Plan

### 本機及靜態建置

1. 記錄 Git HEAD、工作目錄與 remote 狀態。
2. 掃描公開內容，確認無憑證、權杖、個資或不應發布的大型暫存檔。
3. 執行 npm run lint。
4. 執行 npm test；既有正式建置與 24 項測試全部通過。
5. 執行 Pages 靜態建置與輸出驗證。
6. 以正式相同的「/儲存庫名稱/」子路徑啟動本機靜態伺服器。
7. 使用瀏覽器檢查首頁、遊戲、GLB、manifest、service worker、favicon、console 與 network，確認沒有 404 或執行期錯誤。
8. 關閉本機伺服器。

### 防回歸實玩

- 首頁選項與進入遊戲正常。
- 方向鍵、C、R、Q、暫停及重新挑戰正常。
- 四種車模、材質、方向與碰撞不回歸。
- PIT、後援 AI 與 HUD 不因路徑調整而變化。
- 直接重新整理專案首頁仍可啟動。
- PWA 與 service worker scope 只涵蓋 PIT Unit，不影響同帳號其他 Pages 專案。

### GitHub 及正式網址

1. origin 只指向新 PIT Unit 儲存庫。
2. 另一個專案的儲存庫、workflow 與 Pages 網址完全不變。
3. GitHub Actions 測試、建置、artifact 與 deploy job 全部成功。
4. 使用正式網址再做一次瀏覽器與 network 驗證。
5. 更新 walkthrough.md 與 deployment_targets.md，記錄結果及可回復位置。

## Approval Gate

目前只完成研究與 GitHub Pages 遷移規劃。這次同意更新計畫不等於同意修改程式或建立公開儲存庫。

在玩家明確回答三個未決事項並核准執行前：

- 不建立／更新 task.md。
- 不修改程式、設定、測試、資產、README 或 walkthrough。
- 不建立 GitHub 儲存庫、不設定 remote、不推送、不啟用 Pages。
- 不發布或刪除任何網站。
