# PIT Unit V2 — Implementation Plan

## 1. Goal Description

在不破壞第一版既有操作、物理碰撞、嫌犯脫困、天候、車型、重新挑戰與部署方式的前提下，將 PIT Unit 擴充為以訓練模擬為核心的第二版。玩家仍是主要追逐與 PIT 執行者，AI 警車負責補位、包圍、交通控制及必要時接替主追。

第二版首批目標包含：

- 將 PIT 從二元成功判定升級為可解釋的授權、接觸品質、車損、失控、恢復、再次逃逸及包圍結果。
- 加入兩台 AI 後援警車與按鈕式無線電指令圓盤，不要求玩家輸入文字。
- 將駕駛視角移入真正的駕駛室，加入後視鏡及快速回頭；保留後方追蹤，將原本的自動跟車改為直升機視角。
- 修正設定頁左側場景預覽，使城市、鄉間與高速公路選擇會改變預覽內容。
- 建立可由 seed 重現的程序化追逐路網，以固定道路模組產生不同的城市、鄉間與高速公路排列，避免每局完全相同。
- 建立合法免費資產管線與授權清單；優先使用 CC0、公共領域或條款清楚的免費資產，採虛構車款與虛構單位。
- 先完成一個代表性高品質視覺樣板，不在首批一次替換全地圖，也不立即製作完整自由探索開放世界。

### Research Findings

- 現有專案是 React 19、TypeScript、Three.js／React Three Fiber、Rapier 與 vinext Sites 架構，正式部署已有固定 `project_id`，不需要重建專案或更換技術棧。
- `PitGame.tsx` 目前集中管理車模、直線世界、天候、鏡頭、玩家與嫌犯物理、PIT、HUD 及結算；第二版若繼續堆疊在單檔會增加回歸風險，因此需先拆出純規則與獨立元件。
- 現有 `MissionConfig`、`CameraMode`、`Runtime`、`PIT_RULES` 及第一版測試必須保持相容或提供明確遷移，不可破壞現有設定與重玩流程。
- PIT 現在依後側四分之一、相對速度、角度、旋轉與減速判定，且額外施加人工旋轉力；第二版會先記錄實際接觸衝量與力矩，再逐步降低人工補償，不會一次移除而破壞已穩定的碰撞手感。
- 現有駕駛鏡頭只是車體前上方位置，沒有駕駛室、後視鏡或盲區處理；`auto` 是斜後方跟車，而 `chase` 是穩定後方追蹤。
- 設定頁預覽只依日夜與天候切換 class，沒有把 `scene` 傳入視覺 class 或條件內容，因此場景選擇不會改變左側預覽。
- `RoadWorld` 是 2000 公尺直線視覺區塊配固定長護欄，尚無道路連接器、導航圖、路口或串流區塊資料。
- 專案中沒有 `PROJECT_SOUL.md`，因此目前沒有作品定位衝突。

## 2. Open Questions / User Review Required

以下決策已由使用者確認，執行時視為核准基準：

1. **作品定位**：以 PIT 訓練模擬為主，娛樂性服務於可理解、可重玩的訓練體驗。
2. **警車角色**：玩家優先擔任主追與 PIT 執行者；AI 初期只支援、包圍及接替，不主動自由決定 PIT。
3. **無線電操作**：使用按住 `Q` 開啟的按鈕式指令圓盤及快捷鍵，不使用文字輸入。
4. **視角配置**：`driver` 為車內駕駛視角；`chase` 保留後方追蹤；原 `auto` 改為直升機視角。
5. **後方資訊**：駕駛視角加入中央及左右後視鏡，並提供按住 `R` 快速回頭；鏡面採低解析度、較低更新頻率以控制效能。
6. **地圖方向**：使用可重現 seed、固定道路模組與受約束的程序化排列；首批是可分岔、可跨區域的追逐路網，不是完整自由探索開放世界。
7. **資產與預算**：只使用合法免費資產，優先 CC0／公共領域／授權清楚的免費素材；不使用來源不明、擷取自其他遊戲或需要付費的模型。
8. **品牌處理**：先採虛構車款、虛構警徽及虛構執法單位，避免真實品牌與標誌授權風險。
9. **效能目標**：桌上型瀏覽器優先，提供高／中／低畫質；手機與低階裝置先確保可進入與操作，不承諾同等視覺品質。
10. **開發方式**：分里程碑修改，每一段必須保持第一版可玩並通過測試，才進入下一段。

### User Review Notes

- PIT 政策將做成可設定的虛構訓練規範，參考常見風險因素，但不宣稱等同任何特定司法管轄區的正式認證課程。
- 免費資產的品質與內裝完整度不可預先保證；若找不到合格車內模型，先製作通用虛構駕駛室，功能正確後再提升細節。
- 首批會建立程序化地圖核心和少量代表模組；大量民車、公車、火車、完整城市與全面美術替換列為後續擴充，不與核心 PIT 一次上線。
- 目前沒有尚待決定且會阻擋實作的問題。核准本計畫後才會建立 `task.md` 並進入執行。

## 3. Proposed Changes

### Milestone 0 — 保護第一版與共用資料基礎

#### `lib/gameConfig.ts`

- 保留現有 `MissionConfig`、場景、天候及車型 ID。
- 將 `CameraMode` 的內部 ID 保持相容，更新使用者標籤與行為：`chase` 後方追蹤、`driver` 駕駛室、`auto` 直升機。
- 新增鏡頭、鏡面畫質、PIT 政策、損壞、後援單位、道路模組及效能等集中設定，消除第二版 magic numbers。

#### `types/game.ts`（新增）

- 抽出 `Runtime`、車輛快照、PIT 事件、車損、警車角色、無線電命令、道路節點與任務結果型別。
- 對第一版狀態提供預設值，確保重新挑戰與既有設定不因新增欄位失效。

#### `components/game/PitGame.tsx`

- 保留 Canvas、任務生命週期與 HUD 組裝責任。
- 將世界、鏡頭、車輛、PIT、無線電與結算逐步拆出，避免一次大改。
- 高頻物理狀態繼續使用 refs；React state 只更新低頻 HUD 與選單。

### Milestone 1 — 視角與設定頁修正

#### `components/game/CameraRig.tsx`（新增）

- 從 `PitGame.tsx` 抽出鏡頭控制。
- 保留後方追蹤現有穩定行為。
- 將 `auto` 改為直升機視角：根據玩家、嫌犯與後援單位包圍盒調整高度、距離及取景，城市中優先避免遮擋。
- 加入鏡頭與場景障礙物射線檢查；必要時抬高鏡頭，不穿越建築物。

#### `components/game/CockpitView.tsx`（新增）

- 建立通用虛構警車駕駛室、擋風玻璃、儀表板、A 柱與方向盤掛點。
- 鏡頭位於左側駕駛座，方向盤依轉向輸入旋轉。
- 雨天保留未來雨刷／玻璃效果掛點，首批不以高成本玻璃 shader 阻擋功能完成。

#### `components/game/VehicleMirrors.tsx`（新增）

- 中央與左右鏡面使用獨立低解析度 render target。
- 降低更新頻率、裁切遠距物件，低畫質可關閉側鏡或進一步降頻。
- 按住 `R` 時暫時切換快速後看，放開立即平滑返回。

#### `app/page.tsx`

- 在 briefing 根節點加入目前場景 class 或資料屬性。
- 場景預覽依城市、鄉間、高速公路切換建築、植被、護欄、車道與地平線元素；天候與日夜繼續疊加。
- 更新控制提示，加入 `Q` 無線電與 `R` 後看（功能完成後才顯示）。

#### `app/globals.css`

- 新增場景預覽變體與指令圓盤／駕駛 HUD 樣式。
- 視覺切換以 class、CSS transition 與合理權重完成，不新增不必要的 `!important`。

### Milestone 2 — 專業 PIT、風險與車損

#### `lib/pitPolicy.ts`（新增）

- 以案件嚴重度、速度、道路寬度／曲率、天候、能見度、交通量、固定障礙物、車種與後援狀態計算 PIT 風險。
- 回傳 `authorized`、`hold`、`denied` 或 `terminate`，並附可顯示的原因，不以單一固定速度代表所有政策。

#### `lib/pitSimulation.ts`（新增）

- 將接觸位置轉為嫌犯車本地座標。
- 計算相對速度、碰撞衝量、力臂、偏航力矩、接觸持續時間及路面抓地力。
- 將結果分類為擦撞、有效接觸、過度撞擊或不安全撞擊。
- 逐步以 Rapier 接觸資料取代固定人工 torque；保留可調補償係數與回歸測試，避免手感突然退化。

#### `lib/vehicleDamage.ts`（新增）

- 使用元件化狀態：引擎動力、冷卻、轉向、四輪抓地力、懸吊／定位與車身阻力。
- 依碰撞區域、衝量與相對能量分配損壞，不使用單一生命值。
- 損壞實際影響加速、最高速、轉向偏移、抓地力與 AI 決策。

#### `lib/gameRules.ts`

- 保留第一版公開函式，新增第二版規則而非破壞既有測試。
- 將 PIT 結果擴充為 `contained`、`disabled`、`stopped-mobile`、`recovered`、`escaped-containment`、`unsafe-pit`。
- 成功條件包含接觸品質、失控程度、車損及後續包圍，不再只因旋轉與減速立即結束。

#### `components/game/VehicleSimulation.tsx`（新增／抽出）

- 整合玩家、嫌犯、後援警車剛體與損壞效果。
- 嫌犯被 PIT 後依可駕駛性、駕駛性格、包圍缺口及車損決定停止、恢復或再次逃逸。
- 保留現有嫌犯倒車脫困狀態，並使其讀取新損壞參數。

### Milestone 3 — 多警車與按鈕式無線電

#### `lib/pursuitCoordinator.ts`（新增）

- 管理 `primary`、`secondary`、`tertiary`、`containment` 等角色。
- 根據距離、車損、道路位置及玩家命令決定補位或接替。
- PIT 成功時分配車頭與車尾封鎖；失敗時恢復安全跟車順序，避免後援追撞。

#### `components/game/SupportPoliceCar.tsx`（新增）

- 建立兩台 AI 後援警車，共用玩家車輛物理與損壞介面。
- 初期不自行發動 PIT，只執行跟隨、拉開、靠近、接替與包圍。
- 所有高頻控制置於物理更新，不以 React state 逐幀驅動。

#### `components/game/RadioCommandWheel.tsx`（新增）

- 按住 `Q` 開啟、方向或滑鼠選擇、放開送出。
- 首批命令：請求 PIT、準備 PIT、第二單位靠近、封鎖車頭、接替主追、終止追逐。
- 提供按鈕、鍵盤操作、焦點狀態與 aria 標籤；不提供自由文字輸入。
- 指揮中心回覆使用預寫字幕及可合法取得／自行合成的簡短音效。

#### `components/game/DebriefPanel.tsx`（新增）

- 結算顯示授權判斷、接觸品質、公共風險、車損、嫌犯狀態、包圍結果及改進建議。
- 保留「重新挑戰相同 seed」與「返回設定」。

### Milestone 4 — Seeded 程序化追逐路網

#### `lib/seededRandom.ts`（新增）

- 建立不依賴 `Math.random()` 的確定性 PRNG。
- 相同 seed 必須產生相同道路排列、事件與交通初始配置。

#### `lib/roadModules.ts`（新增）

- 定義道路模組入口／出口連接器、方向、車道數、曲率、區域、導航節點、碰撞邊界、PIT 風險與特殊事件掛點。
- 首批模組包含城市直路／彎道／路口、鄉間直路／長彎、高速直線／彎道／交流道及跨區域轉換。

#### `lib/proceduralMap.ts`（新增）

- 依 seed、連接器與約束排列道路模組。
- 防止接縫錯位、立即折返、不可達死路、重疊碰撞體及不合理區域跳轉。
- 產生導航圖供嫌犯與後援 AI 使用，並保存地圖代碼供重玩與除錯。

#### `components/game/ProceduralWorld.tsx`（新增）

- 取代單一 2000 公尺直路，僅保留玩家附近區塊。
- 前方生成、後方回收，使用物件池、InstancedMesh、LOD 與共享材質控制記憶體與 draw calls。
- 首批維持簡化造景；一般民車、公車、火車與完整開放世界列入下一批。

#### `app/page.tsx`

- 增加隨機任務、輸入 seed、重玩相同地圖的設定。
- 原城市／鄉間／高速公路選擇改為起始區域或任務偏好，地圖仍可跨區域。

### Milestone 5 — 合法免費資產代表樣板

#### `public/assets/**`（新增）

- 僅加入經確認可使用的免費 GLB／GLTF、貼圖與音效。
- 優先一台警車、一台嫌犯車、一組道路／護欄及一組代表性環境，不一次下載大量素材。

#### `public/assets/LICENSES.md`（新增）

- 記錄名稱、作者、來源網址、授權、修改內容及必要署名。
- 來源或授權不明的資產不得進入正式版本。

#### `components/game/VehicleModel.tsx` 與環境元件

- 支援免費 GLTF 模型、輪胎／方向盤掛點、警燈與材質覆寫。
- 保留方塊模型 fallback，資產載入失敗時遊戲仍可玩。
- 使用 Draco／Meshopt、KTX2、LOD、材質共用及延遲載入控制下載與 GPU 成本。

### Tests

#### `tests/game-rules.test.mjs`

- 保留全部第一版測試。
- 新增 PIT 授權、接觸分類、多結果、元件損壞、再次逃逸、包圍與不安全 PIT 測試。

#### `tests/procedural-map.test.mjs`（新增）

- 驗證相同 seed 完全一致、不同 seed 產生差異。
- 驗證道路連接、導航可達性、無重疊、無非法死路及區域轉換。

#### `tests/pursuit-coordinator.test.mjs`（新增）

- 驗證玩家主追、AI 補位、PIT 前拉開、成功後包圍、失敗後續追與損壞後接替。

#### `tests/rendered-html.test.mjs`

- 保留正式殼層與 PWA 檢查，補充設定頁場景預覽、seed 輸入及必要無障礙標籤。

## 4. Verification Plan

### 每個里程碑的固定驗證門檻

1. 執行 `npm run lint`。
2. 執行 `npm test`，其中正式 build 必須成功且既有第一版測試不得刪除或放寬。
3. 使用固定 seed 執行可重現測試；任何物理／AI bug 都需記錄 seed 與事件資料。
4. 啟動背景本地伺服器，確認頁面回應成功且無編譯、執行或未處理 Promise 錯誤。
5. 使用 Codex 內建瀏覽器實際操作；若瀏覽器工具故障，必須明確標記未完成，不得以 HTTP 200 或單元測試冒充互動驗證。
6. 驗證完成後關閉本地伺服器。

### Milestone 1 驗證

- `C` 依序切換駕駛、後方追蹤、直升機，且三者定位明確。
- 駕駛鏡頭位於車內；中央／左右後視鏡方向正確，不顯示前方影像。
- `R` 按住後看、放開復位，失焦時不會卡住。
- 後方追蹤保持第一版手感；直升機能同時追蹤主要車輛並減少建築遮擋。
- 城市、鄉間、高速公路選擇會更新左側預覽；日夜與天候仍正確疊加。

### Milestone 2–3 驗證

- 合格 PIT、不合格追尾、錯誤角度、過高風險及未獲授權等情境均得到可解釋結果。
- 車損由碰撞區域與強度決定，會改變操控但不產生 NaN、瞬移或穿透。
- 嫌犯可能停止、恢復或再次逃逸；PIT 接觸本身不再立即宣告完整任務成功。
- `Q` 指令圓盤全程以按鈕／方向選擇，不需打字；鍵盤、滑鼠與焦點操作正常。
- 兩台後援不追撞玩家，不搶先 PIT；成功後封鎖、失敗後續追，玩家車損時可接替主追。
- 重新挑戰保留設定與 seed，返回設定才離開任務。

### Milestone 4 驗證

- 至少以多組固定 seed 驗證城市、鄉間、高速及跨區域排列。
- 所有道路接縫、車道方向、護欄 collider 與導航節點連續。
- 玩家、嫌犯與後援均能通過彎道、路口與交流道；嫌犯不因模組切換停住。
- 長時間追逐不駛出已載入世界，舊區塊會回收，物件與事件監聽器不持續增加。
- 同一 seed 可重現，方便人工回報問題。

### 視覺與效能驗證

- 所有正式資產均能在 `LICENSES.md` 找到來源與授權。
- 資產失敗或低畫質模式仍可使用 fallback 完成任務。
- 檢查桌面常見解析度下 HUD、鏡面、無線電圓盤與結算不互相遮擋。
- 比較鏡面開／關與高／中／低畫質，確認不造成不可接受的持續卡頓或記憶體成長。
- 雨雪、警燈、直升機視角、多警車與程序化區塊同時存在時仍保持可操作。

### Final Walkthrough and Deployment

- 完成核准範圍後建立／更新 `walkthrough.md`，列出完成項目、PIT 模型、AI 行為、地圖 seed、效能處理、免費資產授權及所有測試結果。
- 依 Sites 流程提交已驗證的精確來源、封裝、儲存新版本並發布至既有正式網站。
- 發布成功後再次驗證正式 URL；最後關閉本地伺服器。

## Approval Gate

目前只完成研究與 `implementation_plan.md`。未收到使用者明確回覆「同意」前：

- 不建立或更新 `task.md`。
- 不修改任何遊戲程式、測試、樣式、設定或資產。
- 不安裝依賴、不下載免費資產、不啟動伺服器、不發布新版本。

