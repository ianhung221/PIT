# PIT Unit 開發 Walkthrough

## 完成內容

- 建立可正式部署的 3D 網頁警車 PIT 追逐遊戲。
- 玩家可使用方向鍵或 WASD 操作油門、煞車及轉向。
- 嫌犯車由 AI 控制，會依玩家距離、道路寬度、場景速度與天候抓地力調整逃逸策略。
- PIT 判定會檢查接觸位置、相對速度、後側四分之一碰撞與判定冷卻；直接從車尾追撞不會誤判為有效 PIT。
- 提供駕駛第一人稱、後方追蹤與自動跟車三種鏡頭，遊戲中按 `C` 切換。
- 提供城市、鄉間及高速公路三種場景，具有不同道路寬度、環境物件與 AI 巡航速度。
- 提供白天／黑夜，以及晴朗、暴雨、降雪環境；雨雪會降低玩家與 AI 的抓地力並增加失控幅度。
- 提供巡邏轎車、攔截跑車、警用 SUV，分別具有不同加速、極速、轉向、質量與穩定性。
- 完成速度、倒數、距離、PIT 有效度、鏡頭狀態 HUD，以及暫停、成功、失敗、重新挑戰與返回設定流程。
- 加入 PWA manifest、版本化 service worker 快取與獨立視窗啟動設定。
- 加入網站分享預覽圖、Open Graph 與 X metadata。

## 效能處理

- 遊戲物理狀態使用 mutable refs 在 Three.js render loop 中更新，避免 React 每幀重新渲染。
- HUD 約每 80ms 更新一次，將高頻物理運算與低頻介面狀態分離。
- 雨雪粒子使用單一 `Float32Array` 緩衝區並原地更新，不在每幀建立新物件。
- 遊戲 3D 模組使用動態載入，任務設定首頁不需要先執行完整 Three.js 遊戲程式。
- 道路與地面使用長距離共用幾何；環境配置採確定性資料，避免重新渲染時隨機跳動。
- 鍵盤監聽器在元件卸載時完整清理，並阻止方向鍵造成頁面捲動。

## 驗證結果

- `npm run lint`：通過，無 lint 錯誤。
- `npm test`：通過，4 項測試全部成功。
  - 有效後側 PIT 接觸判定。
  - 直接追撞、速度不足與冷卻期間接觸排除。
  - 雪地低抓地力造成較強旋轉衝量。
  - 正式頁面殼層、標題、語系與 PWA manifest 輸出。
- `npm run build`：正式 Sites／Cloudflare Worker 建置成功。
- 本地伺服器 HTTP 驗證：回應 `200`，完成後已關閉伺服器。
- 正式部署：成功。
- 正式網址：<https://pit-unit-pursuit.ianhung221.chatgpt.site>
- 目前網址採擁有者專用存取；在其他電腦登入相同帳號後可遊玩。若要讓任何人免登入遊玩，需另行確認改為公開存取。
- Codex 內建瀏覽器互動驗證尚待補做：本次工作階段開始時尚未安裝 Node.js，安裝後瀏覽器控制程序未重新載入環境。重新啟動 Codex 工作階段後即可實際點擊設定、進入 3D 場景並測試鍵盤操作。

## 分享圖

- 檔案：`public/og.png`
- 產生方式：內建 ImageGen。
- 最終提示重點：雨夜州際高速公路、原創黑白警車從嫌犯紅色車輛後側接近、寫實低多邊形 3D 遊戲主視覺、紅藍警燈反射、黑／鋼灰／安全橘配色，以及唯一且正確的文字 `PIT UNIT`；禁止真實車廠與警局標誌、武器、傷者及浮水印。

## Physics & Gameplay Fix Pass

### 已修正

- 修正方向鍵正負號：左鍵現在使車輛向左轉，右鍵向右轉，鏡頭只負責跟隨車輛方向。
- 將中心點距離碰撞改為 Rapier 動態剛體與長方體車身 collider，加入 CCD、接觸分離、摩擦、反彈、質量及角動量，車輛不再能互相穿透。
- 三種警車的質量現在真正參與碰撞；SUV 較重、攔截跑車較輕、巡邏轎車居中。
- PIT 接觸位置會先轉換成嫌犯車自身座標，只有後側四分之一、方向差小於 35°、接近速度 1.5–18 m/s 的撞擊會建立 PIT 候選。
- PIT 候選必須在 2.5 秒內讓嫌犯旋轉至少 55°，並降低至少 35% 速度或產生明顯側滑，才會判定成功；同一段接觸不會重複累加。
- 城市、鄉間與高速公路都加入可見護欄與固定 collider，玩家和嫌犯遵守相同邊界；AI 靠近邊界時會提前回正。
- 道路視覺依玩家位置循環回收，長時間追逐不會駛出道路模型；物理護欄覆蓋完整 60 秒可行駛範圍。
- 雨雪粒子會跟隨玩家位置，不再被高速行駛的車輛甩在後方。
- 「重新挑戰」不再重新載入網頁，而是保留相同場景、天候、日夜及車型並重建一局；「返回設定」才回到設定首頁。
- 視窗失焦、頁面隱藏、暫停或重開時會清除按鍵，避免油門／轉向卡住。
- 成功和 60 秒逾時改為互斥結果；成功後立即停止該幀判定，不會再被失敗狀態覆蓋。

### 驗證

- `npm run lint`：通過。
- `npm test`：正式建置成功，6 項測試全部通過。
  - 左右轉向輸入符號。
  - 合格後側四分之一接觸。
  - 追尾、前側撞擊、角度錯誤及危險速度排除。
  - 冷卻與候選期間防止重複計分。
  - 必須同時產生足夠旋轉與失控才算成功。
  - 正式頁面殼層與 metadata。
- 本地開發網址回應 `200`，開發記錄沒有編譯或伺服器執行錯誤。
- 修正版已成功發布至正式網站：<https://pit-unit-pursuit.ianhung221.chatgpt.site>。
- Codex 瀏覽器控制服務本次無法載入其受信任元件，因此沒有宣稱完成實際按鍵操作；發布後仍需以瀏覽器進行人工遊玩確認。

## Suspect AI Recovery Pass

### 已完成

- 嫌犯 AI 會同時檢查低速持續時間、車身偏轉角度、道路邊界位置及前進速度，只在真正卡住時進入恢復流程。
- 恢復流程分為短暫煞車、受控倒車、原地／低速轉正及重新加速四個階段；若第一次未能脫困，會再次循環處理。
- 倒車方向會依左右護欄選擇：車尾先遠離最近護欄，避免倒車時把車身推得更緊。
- 車身偏離道路方向越多，AI 油門越小；接近橫向或掉頭時會先停止加速，對正後才逐步恢復速度。
- 有效 PIT 的 2.5 秒評估期間會暫停脫困計時並沿用原本的低控制力，避免 AI 恢復動作抵銷正確的 PIT 旋轉。
- 修正 `PitGame.tsx` 開頭意外多出的字元，避免下一次建置時造成語法錯誤。

### 驗證

- `npm run lint`：通過。
- `npm test`：正式建置成功，11 項測試全部通過。
- 新增測試涵蓋真卡住判定、正常減速防誤判、PIT 期間抑制、完整恢復狀態轉換、偏轉降速，以及左右護欄的正確倒車方向。
- 本地遊戲頁面回應 `200`，沒有編譯或伺服器執行錯誤。
- AI 脫困修正版已成功發布至正式網站：<https://pit-unit-pursuit.ianhung221.chatgpt.site>。
- Codex 內建瀏覽器執行環境因 Windows 沙箱啟動失敗，未能完成實際碰撞操作；此項保留為人工遊玩確認，不以 HTTP 或單元測試取代。
