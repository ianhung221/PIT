# PIT Unit V2 Walkthrough

## 發布結果

- 第二版已發布到既有私人網站：https://pit-unit-pursuit.ianhung221.chatgpt.site
- 保留原 project、網址與 owner-only 存取設定，沒有改成公開網站。
- 正式首頁已確認顯示 BUILD 0.2 與畫面品質選項。
- 正式 police.glb 資產回應 200，檔案大小 195,336 bytes。

## 完成內容

### 專業 PIT 訓練流程

- PIT 不再因任何碰撞立即成功；必須先取得 authorized 授權。
- 授權會評估速度、天候抓地力、能見度、道路風險、交通密度、障礙物與後援數量。
- 接觸分成 scrape、effective、excessive 與 unsafe。
- 接觸品質會計算嫌犯車本地接觸點、相對速度、有效質量、衝量、力臂、偏航力矩與路面抓地力。
- 固定人工旋轉力已降低，補償量改由計算出的偏航力矩限制後決定。
- 有效接觸後仍須觀察嫌犯車損、是否可繼續行駛，以及後援是否完成包圍。
- 任務結果支援 contained、disabled、stopped-mobile、escaped-containment、unsafe-pit 與 failed。

### 車損與嫌犯 AI

- 車損改為引擎、冷卻、轉向、四輪抓地力、定位與車身等獨立元件。
- 碰撞區域與強度會實際影響加速、極速、轉向與抓地力。
- 嫌犯被 PIT 後可能失能、暫停後再次逃逸，或突破尚未完成的包圍。
- 保留第一版的煞停、倒車、轉正與重新加速脫困流程，並讓車損影響逃逸能力。

### 多警車與無線電

- 加入 02 號巡邏車與 03 號警用 SUV 兩台 AI 後援。
- 玩家預設維持 primary；後援可擔任 secondary、tertiary、containment-front、containment-rear。
- 玩家車況過差或選擇「接替主追」時，02 號會實際移到嫌犯後方接替。
- 有效 PIT 後，兩台後援分別封鎖車頭與車尾；未完成包圍時嫌犯仍可能逃逸。
- Q 可開啟按鈕式無線電圓盤，也可直接點擊畫面上的「無線電指令」。
- 指令包含請求 PIT、準備 PIT、第二單位靠近、封鎖車頭、接替主追與終止追逐。
- 結束畫面新增 PIT 嘗試、有效接觸、未授權次數、嫌犯車況與事件紀錄。

### 視角與後方資訊

- 駕駛視角已移到左側駕駛座，加入通用儀表板、A 柱與方向盤。
- 保留後方追蹤視角。
- 原本的自動跟車已改為直升機視角，會同時框住玩家、嫌犯及兩台後援。
- 駕駛視角具中央與左右後視鏡；按住 R 可快速回頭，放開即恢復。
- 後視鏡使用低解析 render target，避免以完整畫質額外渲染三次。

### 程序地圖與設定

- 任務設定頁的城市、鄉間與高速公路會實際切換左側預覽。
- 新增任務地圖代碼；相同 seed 會重現相同道路排列。
- 道路由固定模組依限制排列，可跨越城市、鄉間與高速公路三個區域。
- 玩家、嫌犯與後援共用道路導航索引。
- 重新挑戰會保留相同 seed；返回設定才離開任務。
- 新增高、中、低畫質。低畫質關閉即時陰影與側鏡。

### 免費合法資產

- 採用 Kenney Car Kit 3.1 的 police、race、suv、sedan-sports 四個 GLB。
- 官方授權為 Creative Commons Zero 1.0。
- 原始授權保存在 public/assets/kenney-car-kit/LICENSE.txt。
- 完整清單保存在 public/assets/LICENSES.md 與 THIRD_PARTY_ASSETS.md。
- GLB 載入失敗時會回退到低多邊形方塊車，不會讓整局無法啟動。

## 效能調整

- 程序道路總長仍保留 72 個可重現區塊，但同時只掛載玩家附近最多 15 個區塊，活躍道路量最多減少約 79%。
- 後視鏡依畫質每 2、3 或 5 幀更新一次；低畫質只保留中央鏡。
- Canvas 像素比依畫質限制為 1.0、1.25 或 1.45。
- 高頻車輛、AI、車損與導航仍在固定物理迴圈及 refs 中處理；React 只以每 0.1 秒更新 HUD。
- 正式建置仍提示 Three.js 主遊戲 chunk 大於 500 kB；這是警告而非建置失敗，後續可再拆分 3D 載入程式。

## 驗證結果

- npm run lint：通過。
- 第一版與第二版規則測試：19 項全部通過。
- 建置後 HTML 測試：1 項通過。
- npm run build：通過。
- 本地首頁：HTTP 200。
- 本地 police.glb：HTTP 200。
- 正式部署：Sites version 4，狀態 succeeded。
- 正式首頁：HTTP 200，確認 BUILD 0.2 與畫面品質。
- 正式 police.glb：HTTP 200，195,336 bytes。
- 本地伺服器已在驗證後關閉。

## 尚未完成的驗證

- Codex 內建互動瀏覽器在多次乾淨重試後，仍因 Windows sandbox helper 初始化錯誤立即關閉。
- 因此沒有宣稱已完成鍵盤駕駛、切換鏡頭、後視鏡方向、PIT 實撞與 AI 包圍的瀏覽器實玩驗證。
- HTTP、lint、build 與自動化測試都不能取代這項人工操作；建議下一步由玩家在正式網址依下方清單實玩。

## 建議實玩清單

1. 以同一 seed 分別選城市、鄉間與高速公路，確認預覽與道路起始區域。
2. 用方向鍵駕駛，確認左右轉向維持第一版正確方向。
3. 按 C 切換後方追蹤、駕駛與直升機視角；駕駛視角按住 R 檢查後方。
4. 按 Q 請求 PIT 授權；比較晴天直路與雨雪高風險區段的回覆。
5. 嘗試未授權碰撞、一般擦撞、後側四分之一有效接觸與過高閉合速度。
6. 有效 PIT 後觀察嫌犯失能、重新逃逸及 02／03 號包圍。
7. 按「相同地圖再試一次」，確認 seed 與設定保留。
8. 分別使用高、中、低畫質檢查順暢度與鏡面差異。

## 後續版本範圍

- 一般民車、公車、火車與交通事件。
- 真正可選分岔的城市路網與完整自由探索大地圖。
- 更多道路、建築、植被、音效、車內細節與逼真材質。
- 更完整的碰撞接觸持續時間、輪胎模型、懸吊與車體變形。

---

# V2 第一輪修正 Walkthrough（2026-09-10）

## 完成內容

- 車模方向與配色：為巡邏車、攔截跑車、警用 SUV 與嫌犯車建立一致的視覺座標校正；GLB 車頭、警燈／車牌與物理碰撞盒的前進方向一致。
- 官方材質補全：補入 Kenney Car Kit 原始的 Textures/colormap.png，恢復車身與警車塗裝；資產授權維持 CC0。
- 駕駛視角：鏡頭直接固定在車輛本地駕駛座座標，不再因加速或減速相對車體前後滑動；駕駛模式會隱藏外部自車模型，避免穿模遮擋。
- 快速後看：駕駛視角按住 R 會直接查看後方，放開後回到前方。
- 後視鏡：中央及左右鏡面相機重新定位；鏡面渲染時暫時排除車內模型，避免白色後座或車身遮住道路。
- 直升機視角：加入可測試的構圖演算法。90 公尺內框住主要追逐車輛；嫌犯拉開後切換成玩家優先，不會再只拍到空道路。

## 驗證結果

- 第一輪修改檔案 ESLint：通過。
- npm test：正式建置成功，24 項測試全部通過。
- 瀏覽器互動：成功進入遊戲並產生 7 張驗證截圖；完成後方追蹤、駕駛視角、加速、R 後看、放開復原及直升機近／遠距離測試。
- 目視結果：車頭與行駛方向一致、配色已恢復；駕駛鏡頭加速時未離開車內；R 後看可見後援；後視鏡可見道路與車輛。
- 直升機近距離：約 87 公尺時可見追逐車輛與後援；遠距離約 291 公尺時仍持續顯示玩家警車。
- 瀏覽器狀態：Canvas 全程存在，最後 HUD 正確顯示「直升機視角」，最終驗證執行期錯誤為 0。
- 本機開發伺服器與隔離 Edge QA 工作階段均已關閉。

## 驗證工具說明

- Codex 內建電腦操作工具再次嘗試後，仍在 Windows sandbox helper 初始化階段退出，未真正載入遊戲。
- 本輪改用獨立的無頭 Edge 除錯工作階段完成相同的按鍵操作與截圖，因此已取得瀏覽器實際執行證據；這個問題不需要玩家調整電腦設定。

## 已知但未納入本輪

- 開發模式會重複輸出 Three.js Clock 與陰影類型的棄用警告；目前不影響遊戲畫面或控制，已記錄為 C-004，建議後續獨立處理。
- 第一輪沒有修改道路接縫、後援 AI、PIT 規則、雪景或漸進式油門／巡航定速；這些維持後續輪次處理。
- 已於 2026-09-11 發布為 Sites 正式版本 5，沿用原網址與私人存取權限：https://pit-unit-pursuit.ianhung221.chatgpt.site

---

# GitHub Pages 永久發布遷移 Walkthrough（2026-09-11）

## 完成內容

- 建立玩家長期持有的公開儲存庫：https://github.com/ianhung221/PIT
- 將目前 D:\Ian\PIT 設為該儲存庫的本機工作目錄；main 已追蹤 origin/main，完整保留既有 Git 歷史。
- 建立 GitHub Pages 靜態輸出流程，正式網址為：https://ianhung221.github.io/PIT/
- 新增 GitHub Actions 自動部署；後續推送 main 會自動執行 lint、測試、靜態建置、輸出驗證及 Pages 發布。
- 將客戶端任務設定畫面與靜態伺服器路由分離，避開 Vinext 靜態預渲染限制；沒有更動畫面或玩法。
- 集中處理 /PIT/ 公開路徑，修正 metadata、favicon、Open Graph、四個 GLB、manifest 與 service worker。
- manifest 與 service worker 的 scope 限定在 /PIT/，不會控制同帳號其他 GitHub Pages 專案。
- 保留原 OpenAI Sites 網址與 .openai/hosting.json，舊站仍可作為備援。
- 更新 README、deployment_targets.md、task.md，說明線上網址、發布方式及多專案連接埠。

## 相容性與效能

- GitHub Pages 版本完全由靜態檔案提供，不需要長期執行 Node／Vinext 伺服器，因此不受 Codex 或學校帳號生命週期影響。
- 本機開發仍使用原本 Vinext 流程；只有 Pages 專用建置會加入 /PIT/ 路徑。
- 遊戲物理、AI、PIT 判定、相機、HUD、模型與資料結構未改動，因此本次不宣稱遊戲 FPS 提升。
- 建置仍會提示主要 3D chunk 大於 500 kB；它不影響本次正確性，之後可另案處理載入效能。

## 驗證結果

- 公開內容安全掃描：未發現 GitHub token、私鑰、API key 或其他憑證。
- npm run lint：通過。
- npm test：正式建置成功，24／24 項測試通過。
- npm run build:pages：成功輸出純靜態首頁，驗證 index.html、PWA 檔案、favicon、Open Graph 圖片及四個 GLB。
- 本機 /PIT/ 預覽：首頁、manifest、service worker、favicon、所有 JS／CSS、四個 GLB、Rapier 與 colormap 均回傳 200。
- 本機無頭 Edge：成功進入 3D 遊戲；視角由「後方追蹤」切至「駕駛視角」；按住 Q 正常顯示無線電圓盤；未捕捉到執行期例外或載入失敗。
- GitHub Actions：https://github.com/ianhung221/PIT/actions/runs/34612913017 已成功完成 build 與 deploy。
- 正式網址 HTTP：首頁、manifest、service worker、favicon 與警車 GLB 均回傳 200。
- 正式網址無頭 Edge：成功進入遊戲、切換視角並開啟無線電圓盤。
- 本機驗證伺服器已關閉。

## 工具狀態與已知事項

- Codex 內建瀏覽器仍因 Windows sandbox helper 初始化錯誤無法啟動；依恢復流程重設後結果相同。
- 本次改用 Microsoft Edge headless 與 DevTools Protocol 完成正式網站互動驗證，因此不是只做 HTTP 檢查。
- Vinext 在 Windows 完成 Pages 預渲染後偶爾於關閉程序時觸發 libuv assertion；建置腳本會先清空舊 dist，且只在 Windows 允許進入嚴格的全新產物驗證。GitHub Linux workflow 仍要求建置正常成功，不會忽略錯誤。

---

# V2 第 2 批剩餘鏡頭修正 Walkthrough（2026-09-14）

## 完成內容

- 將通用駕駛艙與中央／左右後視鏡改成在相機更新後同步姿態，消除加速與減速時因每幀更新順序造成的相對滑動。
- 保留 R 快速後看；後看時移除駕駛艙遮擋，放開後可繼續正常切換視角。
- 將直升機 90 公尺附近的二選一硬切換改成連續權重，後援接近取景邊界時也使用漸進權重。
- 直升機改為斜上方新聞追車式構圖，對取景中心、高度與航向分別平滑；車輛碰撞旋轉時不再直接讓鏡頭瞬間翻轉。
- 遠距離改為玩家優先取景，嫌犯超出共同取景範圍時顯示方向箭頭；完整迷你地圖仍保留至 V3 大地圖階段。
- 修正瀏覽器驗證腳本：Edge 測試資料改放 Windows 暫存區，避免開發伺服器監看被鎖定的瀏覽器快取而中止。

## 效能與相容性

- 鏡頭平滑使用既有逐幀更新與可重用向量，沒有新增每幀 React 狀態或 DOM 重建。
- 後視鏡維持原本依畫質降頻渲染；低畫質仍只顯示中央鏡，中高畫質維持三面鏡。
- 未修改道路、天候、後援 AI、PIT 規則、碰撞體、油門或既有任務資料結構。

## 驗證結果

- `npm run lint`：通過。
- `npm test`：正式建置成功，25／25 項測試通過。
- `npm run build:pages`：GitHub Pages 靜態輸出與 `/PIT/` 子路徑驗證通過。
- 新增直升機 90 公尺臨界點連續性測試：通過。
- 無頭 Edge：成功由後方追蹤切至駕駛視角，持續加速後擷取畫面；駕駛艙與三面鏡保持可見。
- R 快速後看：畫面可見後方道路與後援車；放開後視角輸入未卡住。
- 直升機遠距測試：約 389 公尺時維持玩家警車構圖，嫌犯方向箭頭正常顯示，且未與計時器重疊。
- 瀏覽器過程未回報執行期例外或資源載入失敗。
- 本機驗證伺服器已關閉。
- GitHub Actions run 34855564619：完成且結果為 success。
- 正式網址 `https://ianhung221.github.io/PIT/`：無頭 Edge 成功進入遊戲、加速駕駛視角、使用 R 後看、切換直升機、顯示遠距方向箭頭並開啟 Q 無線電；未回報執行期例外或載入失敗。

## 玩家複驗

- 本批目前標記為 ⚠️，等待玩家在正式 GitHub Pages 網站確認不同車型的加速、煞車、R 後看，以及直升機在碰撞和 90 公尺附近的主觀平滑度。
- 玩家確認前不把第 2 批標成完成，也不開始第 3 批程式修改。

---

# V2 第 3 批道路與天候 Walkthrough（2026-09-16）

## 完成內容

- 玩家已確認第 2 批通過，並於 2026-09-16 核准第 3 批。
- 相鄰路段共用左右端點，跨區域以漸寬／漸窄路面銜接；護欄外觀和碰撞體共用位置、方向及尺寸，接點保留小幅重疊。
- 為兩台後援保存道路索引；世界現在合併四台車各自前後的載入範圍，玩家落後或後援停留原地時仍保有道路與護欄。
- 雨雪使用個別下降速度及保留餘量的循環，不再統一重設至同一高度；粒子扣除玩家實際水平位移，前進、倒車及碰撞側滑都有相對移動。
- 城市／鄉間加入中央虛線，高速公路加入車道虛線，改善直線行駛的速度感。
- 保留既有地圖代碼、道路中心線及隨機排列；生成資料新增 `geometryVersion: 2` 辨認路面幾何版本。新增欄位保持向後相容。
- U-012、U-013 依核准計畫留待 V3；後援 AI、PIT 及油門手感仍依第 4～6 批順序處理。

## 效能與相容性

- 每台車保留後方 3 段、目前段及前方 11 段；四車窗口合併後最多 60 段，不為填滿兩台遠距車輛之間的空間而載入整張地圖。
- 以 0.1 秒間隔檢查道路索引，僅索引改變時更新 React 狀態。
- 每路段全部邊線與虛線合併成 1 個網格；不含造景的道路網格由原本城市／鄉間 7 個、高速公路 9 個降為 5 個。未以此宣稱實際 FPS 提升。
- 雨雪沿用 680 個粒子與同一組座標陣列，每幀不新增粒子物件或 React state；卸載路段時釋放自建幾何。

## 驗證結果

- `npm run lint` 通過；測試腳本後續調整也通過個別 lint。
- 正式建置通過；加入最後一項護欄物理測試後，全套 `node --test tests/*.test.mjs` 為 **30／30 通過**。
- `npm run build:pages` 輸出與 `/PIT/` 子路徑驗證通過。Windows 預渲染結束仍有既有 libuv 關閉警告，既有腳本驗證的是本次新產物；GitHub Linux 發布結果另列下方。
- 道路幾何涵蓋 3 種起始區域、3 組 seed；Rapier 物理測試以車輛撞擊兩處路寬交界的左右護欄，均未穿出。
- 本機無頭 Edge 實玩三組：高速公路／晴天／低畫質、城市／暴雨／中畫質、鄉間／降雪／高畫質，皆成功進入遊戲，使用 C、R、Q 及加速／倒車，最終成功測試沒有執行期例外或網路載入失敗。
- 三組均以瀏覽器控制情境把車隊移到第 24、25、48、49 段接縫附近，再實際按油門通過。此為針對交界的測試，不冒稱整段路全由手動駕駛完成。
- 三組正常運行約 59～63 秒並檢查四車所在路段；嫌犯最遠到第 29 段時，仍落在第 0 段的玩家與後援也有完整載入窗口。
- 雨雪在瀏覽器約 60 秒時仍涵蓋 18 個高度區間；純函式另測 180 秒及大幅掉幀後不會同步成平面。
- 雨天首次倒車斷言未通過；車隊仍在附近，可能受到碰撞干擾。改用隔開其他車輛的控制情境後，按下鍵成功倒車，速度約 -13.6 m/s；沒有為測試更改玩家或 AI 駕駛程式。
- 截圖保存在本機 `work/browser-smoke/`，包含駕駛、R 後看、直升機、道路交界及倒車。

## 發布與玩家回報

- 正式網址：https://ianhung221.github.io/PIT/
- 發布狀態：程式提交 `f104928` 已正式上線；[GitHub Actions 35081780528](https://github.com/ianhung221/PIT/actions/runs/35081780528) 的 Linux 測試、建置及部署全部成功。
- 正式網址無頭 Edge：城市／雨天／中畫質，成功加速、C 切換、R 後看、直升機追蹤、Q 無線電，並再次通過四個交界控制情境及倒車（約 -13.5 m/s）；沒有執行期例外或網路載入失敗。
- 正式站第一次未在原本固定的 8 秒內載入遊戲；驗證腳本已改為等待實際畫面就緒（最多 45 秒），重測成功。這次調整只影響測試腳本。
- 本機預覽伺服器已停止，測試用 Edge 程序已關閉。第 3 批目前等待玩家複驗，第 4 批尚未開始。
- 請在 `v2_acceptance_test.md` 的 D 區 **V2-FIX-007～V2-FIX-010** 填寫結果；保留 A、B、C 區與歷次修改紀錄，不替玩家把狀態改成通過。

---

# V2-FIX-009 天候補修 Walkthrough（2026-09-21）

## 完成內容

- 雨雪粒子的 X、Y、Z、下降速度及雪花飄動改用可重現但彼此獨立的雜湊亂數，消除長時間循環後的數學相關斜向條帶。
- 粒子落出範圍時各自重新選擇水平位置與相位，並保留垂直超出量；既有前進、倒車及側滑的相對位移補償保持不變。
- 雨雪加入可重用的柔邊圓形透明遮罩，主畫面不再繪成生硬方塊。
- 三面後視鏡渲染時暫時縮小雨雪並降低透明度；鏡面完成後立即恢復主畫面材質。鏡面近裁切面也略微外移，降低貼近鏡頭的粒子放大。
- 若鏡面渲染中途發生例外，會恢復 render target、駕駛艙、鏡面及雨雪材質，避免污染後續畫面。
- 未修改道路、鏡頭位置、後援 AI、PIT 規則、操控方式或既有任務資料結構。

## 測試與本機驗證

- `npm run lint`：通過。
- `npm test`：正式建置成功，**32／32** 項自動測試通過。
- 新增固定 seed 重現、亂數通道獨立、二維網格占用率及 80／90／180 秒斜向集中度測試。
- `npm run build:pages`：靜態輸出及 `/PIT/` 子路徑驗證通過；Windows 結束程序時仍出現既有 libuv 關閉警告，但建置腳本確認本次新產物完整。
- 本機無頭 Edge 實玩：鄉間／降雪／高畫質持續到 86.6 秒，雪花涵蓋 108／108 個二維取樣格，未形成斜向條帶；鏡中雪粒尺寸約為主畫面的 18%。
- 本機另測城市／暴雨／中畫質與高速公路／降雪／低畫質；主畫面、中央鏡及中畫質側鏡都沒有大型白色或藍色方塊。
- 三組均成功進入遊戲、切換駕駛及直升機視角並開啟無線電，未捕捉到執行期例外或資源載入失敗。
- 截圖保存在本機 `work/browser-smoke/`；本機預覽伺服器已停止。

## 發布與玩家複驗

- 正式網址：https://ianhung221.github.io/PIT/
- 發布狀態：程式提交 `0f46a1d` 已正式上線；[GitHub Actions 35609503790](https://github.com/ianhung221/PIT/actions/runs/35609503790) 的 Linux 測試、建置及部署全部成功。
- 正式網址無頭 Edge：鄉間／降雪／高畫質成功載入新粒子遮罩與鏡面設定，進入遊戲後完成加速、駕駛／直升機視角切換及 Q 無線電；未回報執行期例外或資源載入失敗。
- 請在 `v2_acceptance_test.md` 的 D 區測試 **V2-FIX-011**；原 V2-FIX-009 的玩家回報保留不改，補修結果用新列永久追蹤。

---

# V2 第 4 批後援 AI 與無線電 Walkthrough（2026-09-23）

## 完成內容

- 玩家確認第三批通過後核准第四批。兩台後援改以各自的道路位置、前視導航點與不同隊形追蹤；遠距時會在既有車型極速內追趕，遇前方慢車會保持距離並視道路空間繞行。
- 02、03 分別保存受阻時間及煞停、倒車、轉正、重新加入追逐的狀態；正常低速跟車不觸發倒車。
- Q2 形成 PIT 支援隊形，Q3 讓 02 靠近，Q5 讓 02 接替主追且 Q2 可交回玩家。Q1 的授權查詢不會覆蓋隊形；Q4 高速時等待，嫌犯明顯減速才派 02 車頭及 03 車尾封鎖；Q6 維持立即終止。
- 指令圓盤避免按鍵自動重複；HUD 顯示繁體中文角色與目前戰術命令，短暫回覆「已接收、執行中、完成、暫時無法執行」。行動後報告保留送出與結果紀錄。
- 本批沒有修改 PIT 接觸、授權門檻、包圍勝利距離或嫌犯再次逃逸門檻；相關問題留第五批。

## 效能與相容性

- 後援導航與狀態計算沿用每幀既有物理更新；沒有新增每幀 React 狀態或道路網格。HUD 仍以既有約 0.1 秒節奏更新。
- 每台後援的速度受 patrol／SUV 原有極速限制；不使用瞬移。雨雪抓地力會降低加減速及脫困速度。
- 玩家在 `v2_acceptance_test.md` 的歷史回報與原 V2-FIX-009 ⚠️ 保留；第四批另新增 V2-FIX-012～015。

## 驗證結果

- `npm run lint` 通過；39 項規則與回歸測試通過，正式建置與 GitHub Pages `/PIT/` 輸出通過。
- 本機無頭 Edge：鄉間晴天、鄉間雪天、城市雨天控制情境，02、03 均完成撞牆後煞停、倒車、轉正及繼續追逐。雪天遠距追趕時，與嫌犯的距離由約 217／235 公尺縮至 83／66 公尺；城市雨天由約 218／236 公尺縮至 37／60 公尺。
- Q1 不取消 Q5 接替；Q2 交回玩家主追；Q4 高速等待、低速到位後顯示完成。另以固定位置情境檢查 Q2、Q3、Q5 均在後援實際到位後才顯示完成。
- 瀏覽器測試包含鍵盤、滑鼠與長按數字鍵；無執行期例外或資源載入失敗。本機截圖留在 `work/browser-smoke/`。
- 遠距追趕與前車避讓分開驗證。後援通過慢速玩家後可加速，但低抓地力及車型極速仍會讓追上需要時間；不宣稱所有交通配置都能在任務時限內追上。
- 完整 `tsc --noEmit` 仍報告原有 CameraRig 推論型別與 Cloudflare worker 型別錯誤；本次修改檔案未被列為錯誤來源。正式建置、lint 與遊戲實玩均通過。

## 發布與玩家複驗

- 正式網址：https://ianhung221.github.io/PIT/
- 發布工作與正式網址結果待補記。玩家請在 `v2_acceptance_test.md` 的 D 區複驗 V2-FIX-012～015。
